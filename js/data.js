/* ============================================================
   AUTO-GENERATED — do not edit by hand.
   Source of truth: data/*.json
   Regenerate:  node scripts/build-data-bundle.mjs

   This bundle lets index.html run when opened directly from
   disk (file://), where fetch() of local JSON is blocked.
   ============================================================ */
window.REFERENCE_DATA = {"corpus":[{"id":"open_with","code":"with open(\"file.txt\", encoding=\"utf-8\") as f: ...","stage":"load","short":"Open a file in a context that guarantees closure.","what":"Python's built-in mechanism for working with a file. The <code>with</code> block ensures the file is closed automatically when the block ends, even if an exception is raised.","tokens":[["open(...)","Built-in function returning a file object."],["\"file.txt\"","The path to the file."],["encoding=\"utf-8\"","The character encoding used to interpret the bytes."],["as f","A binding of the file object to the name <code>f</code> for the duration of the block."]],"requires":["a path to a text file"],"important":["the file is not tabular and pandas loaders do not apply","line-by-line iteration is desired"],"avoid":["calling <code>open</code> without <code>with</code> in interactive work (forgetting to close)"],"reach":"When the file is plain text without a tabular structure, or when one needs streaming access.","example":"<pre><span class=\"k\">with</span> open(<span class=\"s\">\"notes.txt\"</span>, encoding=<span class=\"s\">\"utf-8\"</span>) <span class=\"k\">as</span> f:\n    <span class=\"k\">for</span> line <span class=\"k\">in</span> f:\n        <span class=\"k\">print</span>(line.strip())</pre>","tags":["load","file","open","text","context","stream","datei"]},{"id":"Path","code":"Path(\"data\") / \"encounters.csv\"","stage":"load","short":"Construct a file path in an OS-independent manner.","what":"Builds a path object using the <code>pathlib</code> module. The <code>/</code> operator joins path components irrespective of the operating system's separator.","tokens":[["Path(...)","A class from <code>pathlib</code> representing a filesystem path."],["\"data\"","The first component of the path."],["/","Operator that joins path components."]],"requires":["<code>from pathlib import Path</code>"],"important":["the code may run on multiple operating systems","paths are built from several components"],"avoid":["hard-coding paths with backslashes or forward slashes in string form"],"reach":"Whenever a path is constructed; preferable to string concatenation in all cases.","example":"<pre><span class=\"k\">from</span> pathlib <span class=\"k\">import</span> Path\np = Path(<span class=\"s\">\"data\"</span>) / <span class=\"s\">\"encounters.csv\"</span>\ndf = pd.read_csv(p)</pre>","tags":["load","path","pathlib","file","windows","os"]},{"id":"read_csv","code":"pd.read_csv(\"file.csv\")","stage":"load","short":"Read a comma-separated file into a DataFrame.","what":"Reads a delimited text file from disk and returns a pandas <span class='glossary-link' data-term='DataFrame'>DataFrame</span> in memory. The default delimiter is a comma and the default encoding is UTF-8.","tokens":[["pd","The conventional alias for the pandas library after <code>import pandas as pd</code>."],[".read_csv(...)","A pandas function that parses a text file containing delimited records."],["\"file.csv\"","A path to the file. May be absolute or relative to the working directory of the notebook."]],"requires":["pandas imported (<code>import pandas as pd</code>)","a file path that resolves to a readable file"],"important":["the delimiter is not a comma (set <code>sep=';'</code>)","the file uses non-UTF-8 encoding (set <code>encoding='latin-1'</code> or <code>'cp1252'</code>)","date columns should be parsed at load time (<code>parse_dates=[...]</code>)"],"avoid":["loading a file larger than available memory without <code>nrows=</code> or <code>chunksize=</code>","ignoring encoding errors silently"],"reach":"When the input is a delimited text file and the entire table is intended to be present in memory.","example":"<pre>df = pd.read_csv(<span class=\"s\">\"data/encounters.csv\"</span>,\n                 encoding=<span class=\"s\">\"utf-8\"</span>,\n                 sep=<span class=\"s\">\",\"</span>,\n                 parse_dates=[<span class=\"s\">\"admission_date\"</span>])</pre>","tags":["load","read","import","csv","file","delimited","comma","datei","laden","einlesen"]},{"id":"read_excel","code":"pd.read_excel(\"file.xlsx\", sheet_name=\"Q1\")","stage":"load","short":"Read an Excel sheet into a DataFrame.","what":"Reads a single sheet of an Excel workbook into a DataFrame. Requires the auxiliary package <code>openpyxl</code> for <code>.xlsx</code> files.","tokens":[[".read_excel(...)","Pandas function that parses an Excel workbook."],["\"file.xlsx\"","The path to the workbook."],["sheet_name=\"Q1\"","Which sheet to read. May also be an integer index or <code>None</code> to obtain a dictionary of all sheets."]],"requires":["<code>openpyxl</code> installed","a workbook with at least one readable sheet"],"important":["the workbook contains formulas (values are read, not formulas)","multiple sheets are required (use <code>sheet_name=None</code>)"],"avoid":["relying on cell formatting (colour, font) as data — it is not preserved"],"reach":"When the source data is an Excel workbook, particularly when the file already contains tabular structure rather than complex layout.","example":"<pre>df = pd.read_excel(<span class=\"s\">\"data/staffing_2026Q1.xlsx\"</span>,\n                   sheet_name=<span class=\"s\">\"Q1\"</span>)</pre>","tags":["load","excel","xlsx","sheet","workbook","arbeitsblatt"]},{"id":"read_json","code":"pd.read_json(\"file.json\")","stage":"load","short":"Read a JSON document into a DataFrame.","what":"Parses a JSON file or JSON string into a DataFrame. The structure of the JSON determines how rows and columns are inferred.","tokens":[[".read_json(...)","Pandas function for JSON input."],["\"file.json\"","The path or URL of the JSON resource."]],"requires":["a JSON document whose structure is convertible to tabular form"],"important":["the JSON is deeply nested (consider <code>pd.json_normalize</code> instead)","records are stored under a key (<code>orient='records'</code> or extracting first)"],"avoid":["reading very large JSON streams without preprocessing"],"reach":"When data is delivered by an API or stored in a JSON file with a tabular structure.","example":"<pre>df = pd.read_json(<span class=\"s\">\"data/triage.json\"</span>)</pre>","tags":["load","json","api","read","import"]},{"id":"read_parquet","code":"pd.read_parquet(\"file.parquet\")","stage":"load","short":"Read a Parquet file into a DataFrame.","what":"Reads a columnar binary format optimised for analytical workloads. Considerably faster and smaller than CSV for the same data.","tokens":[[".read_parquet(...)","Pandas function for Parquet input."],["\"file.parquet\"","The path to the Parquet file or directory."]],"requires":["<code>pyarrow</code> or <code>fastparquet</code> installed"],"important":["working with intermediate files between Python steps","data exceeds approximately 10⁶ rows"],"avoid":["sharing data with non-technical recipients (Parquet is not human-readable)"],"reach":"For intermediate storage between analysis steps, or when working with files generated by data engineering pipelines.","example":"<pre>df = pd.read_parquet(<span class=\"s\">\"data/encounters.parquet\"</span>)</pre>","tags":["load","parquet","columnar","binary","fast"]},{"id":"describe","code":"df.describe()","stage":"inspect","short":"Return descriptive statistics for numeric columns.","what":"Returns count, mean, standard deviation, minimum, the three quartiles, and maximum for each numeric column. With <code>include='all'</code>, non-numeric columns are also summarised.","tokens":[[".describe()","Method returning a summary table."]],"requires":["a DataFrame containing at least one numeric column"],"important":["one wishes to see the central tendency, spread, and range of every numeric variable in a single view"],"avoid":["interpreting <code>describe</code> on categorical columns without <code>include='all'</code>"],"reach":"For an initial impression of the distribution of every numeric variable.","example":"<pre>df.describe()\ndf.describe(include=<span class=\"s\">\"all\"</span>)</pre>","tags":["inspect","describe","stats","statistics","summary","mean","median","quartile"]},{"id":"dtypes","code":"df.dtypes","stage":"inspect","short":"Return the data type of each column.","what":"Returns a Series mapping column names to their <span class='glossary-link' data-term='dtype'>dtype</span>. An attribute, not a method.","tokens":[[".dtypes","Attribute returning a Series of dtypes per column."]],"requires":["a DataFrame"],"important":["one suspects a column was loaded as <code>object</code> when a numeric type was expected"],"avoid":[],"reach":"To verify that types were inferred correctly, particularly after loading from CSV.","example":"<pre>df.dtypes</pre>","tags":["inspect","types","dtypes","datentyp"]},{"id":"duplicated","code":"df.duplicated().sum()","stage":"inspect","short":"Count duplicate rows.","what":"<code>duplicated()</code> returns a boolean Series marking rows that repeat a previous row; <code>sum()</code> counts the marked rows.","tokens":[[".duplicated()","Returns <code>True</code> for rows that are exact repetitions of an earlier row."],[".sum()","Counts the <code>True</code> values."]],"requires":["a DataFrame"],"important":["the data is an export from another system that may have produced repeated rows"],"avoid":["assuming uniqueness without checking <code>duplicated(subset=[key])</code> for the key column"],"reach":"Routinely after loading from an external system.","example":"<pre>df.duplicated().sum()\ndf.duplicated(subset=[<span class=\"s\">\"patient_id\"</span>]).sum()</pre>","tags":["inspect","duplicates","duplicated","unique","duplikat"]},{"id":"head","code":"df.head(10)","stage":"inspect","short":"Return the first n rows.","what":"Returns the first <em>n</em> rows of the DataFrame as a smaller DataFrame. The default is five.","tokens":[["df","The DataFrame."],[".head(...)","Method returning the leading rows."],["10","The number of rows to return; optional."]],"requires":["a DataFrame"],"important":["one wishes to inspect the structure of the data without printing the entire table"],"avoid":["relying on <code>head</code> to identify anomalies — the leading rows are not a random sample"],"reach":"To verify that the loader produced the expected columns, and to read example values.","example":"<pre>df.head()</pre>","tags":["inspect","head","preview","first","top","anfang","vorschau"]},{"id":"info","code":"df.info()","stage":"inspect","short":"Print a concise summary of the DataFrame.","what":"Prints, for each column, its name, the count of non-null values, and the <span class='glossary-link' data-term='dtype'>dtype</span>. Also reports total memory use. The output is printed, not returned.","tokens":[["df","The DataFrame under inspection."],[".info()","A method that prints structural information."]],"requires":["a DataFrame object"],"important":["one wishes to see, in a single view, types and missingness across all columns"],"avoid":["expecting a return value to assign — the method prints rather than returns"],"reach":"As the second check after loading, when one wishes a single view of types and non-null counts.","example":"<pre>df.info()</pre>","tags":["inspect","info","types","dtypes","summary","strukturuebersicht"]},{"id":"isna_sum","code":"df.isna().sum()","stage":"inspect","short":"Count missing values per column.","what":"Composes two methods: <code>isna()</code> returns a boolean DataFrame in which <code>True</code> marks missing cells; <code>sum()</code> then totals the <code>True</code> values along each column.","tokens":[[".isna()","Returns a boolean DataFrame with <code>True</code> where the value is <code>NaN</code>."],[".sum()","Sums the boolean values along columns (treating <code>True</code> as 1)."]],"requires":["a DataFrame"],"important":["one wishes to know, for every column, how many values are missing","precedes any decision about imputation or dropping"],"avoid":[],"reach":"Routinely after loading, to determine the prevalence of missingness in each column.","example":"<pre>df.isna().sum()\ndf.isna().mean()   <span class=\"c\"># proportion missing</span></pre>","tags":["inspect","missing","nan","null","isna","isnull","fehlend","leer"]},{"id":"sample","code":"df.sample(5)","stage":"inspect","short":"Return a random sample of n rows.","what":"Returns a random sample of <em>n</em> rows. Unlike <code>head</code>, the sample is not biased toward the start of the file.","tokens":[[".sample(...)","Method returning a random subset of rows."],["5","The sample size."]],"requires":["a DataFrame"],"important":["the data is sorted or grouped at load time and <code>head</code> would not be representative"],"avoid":["sampling without <code>random_state</code> when reproducibility is needed"],"reach":"When inspecting a file whose order is not random, particularly large export files.","example":"<pre>df.sample(<span class=\"s\">5</span>, random_state=<span class=\"s\">42</span>)</pre>","tags":["inspect","sample","random","preview","stichprobe"]},{"id":"shape","code":"df.shape","stage":"inspect","short":"Return the dimensions of the DataFrame.","what":"Returns a two-tuple containing the number of rows and the number of columns. An attribute, not a method — therefore no parentheses.","tokens":[["df","The DataFrame under inspection."],[".shape","Attribute returning <code>(n_rows, n_columns)</code>."]],"requires":["a DataFrame or Series object"],"important":["the first check after loading; confirms the loader produced the expected size"],"avoid":["confusing with <code>.size</code>, which returns the total number of cells"],"reach":"Immediately after loading, and after any operation that may change the number of rows.","example":"<pre>df.shape   <span class=\"c\"># e.g. (320, 8)</span></pre>","tags":["inspect","shape","size","rows","columns","dim","dimension","groesse"]},{"id":"value_counts","code":"df[\"col\"].value_counts()","stage":"inspect","short":"Count occurrences of each unique value in a column.","what":"Returns a Series whose index is the unique values of the column and whose values are the counts, sorted descending.","tokens":[["df[\"col\"]","Selects the column named <code>col</code> from the DataFrame, returning a Series."],[".value_counts()","Method counting the frequency of each unique value."]],"requires":["a column (Series), typically categorical or of low cardinality"],"important":["one wishes to inspect the categories present in a column, and their balance","missing values should be counted explicitly (use <code>dropna=False</code>)"],"avoid":["calling on a continuous numeric column with many distinct values (produces a long, unhelpful list)"],"reach":"For any categorical or low-cardinality column, to inspect the categories present and their balance.","example":"<pre>df[<span class=\"s\">\"ward\"</span>].value_counts()\ndf[<span class=\"s\">\"gender\"</span>].value_counts(dropna=<span class=\"k\">False</span>)</pre>","tags":["inspect","count","frequency","categorical","values","value_counts","haeufigkeit"]},{"id":"astype","code":"df[\"age\"].astype(int)","stage":"clean","short":"Cast a column to a specified type.","what":"Returns a Series whose values have been converted to the requested type. Fails on values that cannot be converted (including <code>NaN</code> for integer types).","tokens":[[".astype(...)","Method casting to a new dtype."],["int","The target type. May also be <code>float</code>, <code>str</code>, <code>'category'</code>, or a numpy dtype."]],"requires":["a column whose values are convertible to the target type","no <code>NaN</code> when casting to <code>int</code>"],"important":["a column was loaded as <code>object</code> when a numeric type was expected","memory savings are desired (cast to <code>'category'</code>)"],"avoid":["casting a column with <code>NaN</code> to <code>int</code> (use <code>'Int64'</code> instead, which supports missing values)"],"reach":"When a column's inferred type does not match its intended use.","example":"<pre>df[<span class=\"s\">\"age\"</span>] = df[<span class=\"s\">\"age\"</span>].astype(<span class=\"s\">\"Int64\"</span>)\ndf[<span class=\"s\">\"ward\"</span>] = df[<span class=\"s\">\"ward\"</span>].astype(<span class=\"s\">\"category\"</span>)</pre>","tags":["clean","astype","cast","convert","type","umwandeln"]},{"id":"clip","code":"df[\"x\"].clip(lower=0, upper=120)","stage":"clean","short":"Constrain values to a specified range.","what":"Returns a Series in which values below <code>lower</code> are set to <code>lower</code> and values above <code>upper</code> are set to <code>upper</code>.","tokens":[[".clip(...)","Method bounding values to a range."],["lower=0","The lower bound."],["upper=120","The upper bound."]],"requires":["a numeric Series"],"important":["the domain has known physical bounds (age, percentages)","outliers are known to be input errors"],"avoid":["clipping silently — values at the bound become indistinguishable, which may bias subsequent statistics"],"reach":"When values outside a plausible range are best treated as the bound rather than dropped.","example":"<pre>df[<span class=\"s\">\"age\"</span>] = df[<span class=\"s\">\"age\"</span>].clip(lower=<span class=\"s\">0</span>, upper=<span class=\"s\">120</span>)</pre>","tags":["clean","clip","outlier","bound","range","ausreisser"]},{"id":"drop_duplicates","code":"df.drop_duplicates(subset=[\"patient_id\"], keep=\"last\")","stage":"clean","short":"Remove duplicate rows.","what":"Returns a DataFrame in which rows whose values in the <code>subset</code> columns repeat earlier rows are removed. <code>keep</code> selects which occurrence to retain.","tokens":[[".drop_duplicates(...)","Method removing duplicate rows."],["subset=[\"patient_id\"]","Restricts the duplicate check to specified columns."],["keep=\"last\"","Retains the last occurrence rather than the first."]],"requires":["a DataFrame"],"important":["the data are exported from a system whose updates produce successive rows for the same entity"],"avoid":["applying without <code>subset</code> when only certain columns define identity"],"reach":"After loading, when the source system may produce repeated records.","example":"<pre>df = df.drop_duplicates(subset=[<span class=\"s\">\"patient_id\"</span>], keep=<span class=\"s\">\"last\"</span>)</pre>","tags":["clean","duplicates","drop","unique","duplikat","entfernen"]},{"id":"dropna","code":"df.dropna(subset=[\"col\"])","stage":"clean","short":"Remove rows that contain missing values.","what":"Returns a DataFrame from which rows containing <code>NaN</code> in the specified columns have been removed. Without <code>subset</code>, any <code>NaN</code> in any column causes the row to be removed.","tokens":[[".dropna(...)","Method removing rows with missing values."],["subset=[\"col\"]","Restricts the check to specified columns; only <code>NaN</code> in these columns triggers removal."]],"requires":["a DataFrame containing at least one <code>NaN</code>"],"important":["the column listed in <code>subset</code> is essential to the analysis","the loss of rows is acceptable (typically below five per cent)"],"avoid":["calling <code>dropna()</code> with no arguments on real data — it removes any row with any missing value, often discarding most of the table"],"reach":"When a value in an essential column is missing and no defensible substitute exists.","example":"<pre>df_clean = df.dropna(subset=[<span class=\"s\">\"patient_id\"</span>, <span class=\"s\">\"admission_date\"</span>])</pre>","tags":["clean","drop","remove","missing","nan","dropna","zeile loeschen","entfernen"]},{"id":"ffill","code":"df.ffill()","stage":"clean","short":"Propagate the most recent known value forward.","what":"Replaces each <code>NaN</code> with the most recent non-missing value preceding it. <code>bfill</code> propagates backward.","tokens":[[".ffill()","Forward fill."]],"requires":["data with a meaningful order (typically time)"],"important":["the underlying state changes infrequently and the last observation is a reasonable proxy"],"avoid":["filling across long gaps where the state is likely to have changed"],"reach":"For sparse measurements where the most recent reading is the best available estimate.","example":"<pre>df = df.sort_values(<span class=\"s\">\"timestamp\"</span>).ffill()</pre>","tags":["clean","ffill","forward fill","bfill","missing","time"]},{"id":"fillna","code":"df[\"col\"].fillna(value)","stage":"clean","short":"Replace missing values with a specified substitute.","what":"Returns a Series (or DataFrame) in which <code>NaN</code> is replaced by the value supplied. The substitute may be a scalar, a dictionary mapping columns to values, or another Series aligned on the index.","tokens":[["df[\"col\"]","Selects the column to operate on."],[".fillna(...)","Method replacing missing values."],["value","The substitute. For numeric columns, frequently <code>df['col'].median()</code>; for categorical, a sentinel such as <code>'unknown'</code>."]],"requires":["a Series or DataFrame containing <code>NaN</code>","a defensible substitute value"],"important":["the missingness is plausibly random rather than informative","the column is numeric and a central tendency provides a sensible default"],"avoid":["filling with zero unless zero is a meaningful default in the domain","filling more than approximately a third of a column without acknowledging the assumption"],"reach":"When a missing value can be defensibly replaced rather than dropped or allowed to propagate.","example":"<pre>df[<span class=\"s\">\"age\"</span>] = df[<span class=\"s\">\"age\"</span>].fillna(df[<span class=\"s\">\"age\"</span>].median())\ndf = df.fillna({<span class=\"s\">\"gender\"</span>: <span class=\"s\">\"unknown\"</span>, <span class=\"s\">\"age\"</span>: df[<span class=\"s\">\"age\"</span>].median()})</pre>","tags":["clean","fill","fillna","impute","missing","nan","replace","auffuellen","ersetzen"]},{"id":"interpolate","code":"df[\"x\"].interpolate()","stage":"clean","short":"Estimate missing values by interpolation between known values.","what":"Replaces <code>NaN</code> with values estimated from neighbouring known values. The default is linear interpolation.","tokens":[[".interpolate()","Method computing interpolated replacements."]],"requires":["a numeric Series with known values on both sides of the missing cells"],"important":["the data are ordered (typically time-indexed)","the underlying process is plausibly continuous"],"avoid":["interpolating across long stretches of missing data","using on categorical columns"],"reach":"For time series with isolated missing values within an otherwise continuous record.","example":"<pre>df[<span class=\"s\">\"temperature\"</span>] = df[<span class=\"s\">\"temperature\"</span>].interpolate()</pre>","tags":["clean","interpolate","missing","nan","time series","zeitreihe"]},{"id":"replace","code":"df[\"x\"].replace({\"M\": \"male\", \"F\": \"female\"})","stage":"clean","short":"Map specific values to replacements.","what":"Returns a Series in which exact-match values are replaced by their counterparts in the mapping dictionary.","tokens":[[".replace(...)","Method substituting values."],["{\"M\": \"male\", \"F\": \"female\"}","A mapping from old values to new values."]],"requires":["a Series and a mapping"],"important":["categorical encoding must be made consistent","sentinel codes (-99, 999) should be mapped to <code>NaN</code>"],"avoid":["using <code>replace</code> for partial-string substitution — use <code>str.replace</code>"],"reach":"When category labels must be standardised to a canonical form.","example":"<pre><span class=\"k\">import</span> numpy <span class=\"k\">as</span> np\ndf[<span class=\"s\">\"score\"</span>] = df[<span class=\"s\">\"score\"</span>].replace({<span class=\"s\">-99</span>: np.nan})</pre>","tags":["clean","replace","map","recode","mapping","ersetzen"]},{"id":"str_strip","code":"df[\"x\"].str.strip().str.lower()","stage":"clean","short":"Normalise string values.","what":"The <code>.str</code> accessor exposes string methods that operate element-wise. <code>.strip()</code> removes leading/trailing whitespace; <code>.lower()</code> converts to lowercase.","tokens":[[".str","Accessor for vectorised string operations on a Series of strings."],[".strip()","Removes leading and trailing whitespace."],[".lower()","Converts to lowercase."]],"requires":["a Series of strings"],"important":["the same category appears in several spellings","whitespace from an export distorts grouping"],"avoid":["chaining many string operations without intermediate inspection"],"reach":"Before any operation that depends on string equality (grouping, joining, filtering by value).","example":"<pre>df[<span class=\"s\">\"gender\"</span>] = (df[<span class=\"s\">\"gender\"</span>]\n                  .str.strip()\n                  .str.lower()\n                  .replace({<span class=\"s\">\"m\"</span>: <span class=\"s\">\"male\"</span>, <span class=\"s\">\"w\"</span>: <span class=\"s\">\"female\"</span>}))</pre>","tags":["clean","string","str","strip","lower","normalize","whitespace","kategorie"]},{"id":"to_datetime","code":"pd.to_datetime(df[\"date\"], errors=\"coerce\")","stage":"clean","short":"Parse a column of date strings into datetime objects.","what":"Converts strings to <code>datetime64</code>. With <code>errors='coerce'</code>, unparseable values become <code>NaT</code> (not-a-time).","tokens":[["pd.to_datetime(...)","Function parsing dates."],["errors=\"coerce\"","Failures become <code>NaT</code> rather than raising."]],"requires":["a column whose values represent dates"],"important":["date arithmetic, sorting, or time-based aggregation is required"],"avoid":["relying on auto-inference for ambiguous formats (provide <code>format=</code> explicitly)"],"reach":"Whenever a column representing dates was loaded as text.","example":"<pre>df[<span class=\"s\">\"admission_date\"</span>] = pd.to_datetime(\n    df[<span class=\"s\">\"admission_date\"</span>], format=<span class=\"s\">\"%Y-%m-%d\"</span>, errors=<span class=\"s\">\"coerce\"</span>)</pre>","tags":["clean","datetime","date","parse","convert","datum"]},{"id":"to_numeric","code":"pd.to_numeric(df[\"x\"], errors=\"coerce\")","stage":"clean","short":"Convert a column to numeric, coercing failures to NaN.","what":"Converts strings to numbers. With <code>errors='coerce'</code>, values that cannot be parsed become <code>NaN</code> rather than raising an exception.","tokens":[["pd.to_numeric(...)","Function converting to a numeric type."],["errors=\"coerce\"","Values that fail conversion are replaced by <code>NaN</code>."]],"requires":["a column whose values are mostly numeric in string form"],"important":["the column was loaded as string (<code>object</code>) due to stray non-numeric values"],"avoid":["coercing silently in production data without auditing the resulting <code>NaN</code> count"],"reach":"When numeric data have been polluted by stray text (e.g. a currency sign, a footnote marker).","example":"<pre>df[<span class=\"s\">\"cost\"</span>] = pd.to_numeric(df[<span class=\"s\">\"cost\"</span>], errors=<span class=\"s\">\"coerce\"</span>)</pre>","tags":["clean","numeric","convert","coerce","to_numeric"]},{"id":"cut","code":"pd.cut(df[\"age\"], bins=[0,18,65,120], labels=[\"child\",\"adult\",\"senior\"])","stage":"transform","short":"Bin a continuous variable into named ranges.","what":"Returns a categorical Series in which each value is assigned to the bin whose range contains it.","tokens":[["pd.cut(...)","Function binning continuous values."],["bins=[...]","The bin edges."],["labels=[...]","Names assigned to the resulting categories."]],"requires":["a numeric Series","bin edges that cover the range of values"],"important":["a continuous variable must be summarised by category for plotting or grouping"],"avoid":["forgetting that bins are right-inclusive by default"],"reach":"When age, time, or any continuous variable should be discretised for analysis.","example":"<pre>df[<span class=\"s\">\"age_group\"</span>] = pd.cut(df[<span class=\"s\">\"age\"</span>],\n                          bins=[<span class=\"s\">0</span>,<span class=\"s\">18</span>,<span class=\"s\">65</span>,<span class=\"s\">120</span>],\n                          labels=[<span class=\"s\">\"child\"</span>,<span class=\"s\">\"adult\"</span>,<span class=\"s\">\"senior\"</span>])</pre>","tags":["transform","cut","bin","discretize","categories","klassieren"]},{"id":"filter_mask","code":"df[df[\"age\"] > 65]","stage":"transform","short":"Filter rows by a boolean condition.","what":"Returns a DataFrame containing only those rows for which the boolean expression evaluates to true. The expression is evaluated element-wise across the column.","tokens":[["df[\"age\"] > 65","A boolean Series — <code>True</code> where age exceeds 65."],["df[ ... ]","Selects rows where the supplied mask is <code>True</code>."]],"requires":["a column suitable for comparison"],"important":["combining multiple conditions: each must be parenthesised, and the operators are <code>&amp;</code> (and) and <code>|</code> (or)"],"avoid":["using Python's <code>and</code> / <code>or</code> instead of <code>&amp;</code> / <code>|</code>","writing back into a filtered view (use <code>.loc</code> for assignment)"],"reach":"For any row-level filter on a single or compound condition.","example":"<pre>df[(df[<span class=\"s\">\"age\"</span>] &gt; <span class=\"s\">65</span>) &amp; (df[<span class=\"s\">\"ward\"</span>] == <span class=\"s\">\"ICU\"</span>)]</pre>","tags":["transform","filter","mask","subset","where","boolean","zeile filtern"]},{"id":"isin","code":"df[df[\"drg\"].isin([\"A01\",\"A02\"])]","stage":"transform","short":"Filter rows by membership in a set.","what":"<code>isin(values)</code> returns a boolean Series, true where the column's value appears in the supplied collection.","tokens":[[".isin([...])","Returns <code>True</code> where the value is in the list."]],"requires":["a column and a list (or set) of acceptable values"],"important":["one wishes to keep rows matching any of several categories"],"avoid":["using a generator expression — <code>isin</code> requires a concrete collection"],"reach":"For a set-membership filter — preferable to chained <code>|</code> comparisons.","example":"<pre>df[df[<span class=\"s\">\"drg_code\"</span>].isin([<span class=\"s\">\"A01\"</span>,<span class=\"s\">\"A02\"</span>,<span class=\"s\">\"B05\"</span>])]</pre>","tags":["transform","filter","isin","set","membership","in"]},{"id":"new_col","code":"df[\"new\"] = df[\"a\"] - df[\"b\"]","stage":"transform","short":"Create a new column by vectorised arithmetic.","what":"Assigns a new column to the DataFrame, computed element-wise from existing columns. The arithmetic is applied to every row in a single operation.","tokens":[["df[\"new\"] = ...","Creates (or overwrites) the column named <code>new</code>."],["df[\"a\"] - df[\"b\"]","Element-wise difference of two columns."]],"requires":["the columns referenced exist and contain compatible types"],"important":["the computation is the same for every row","performance matters (vectorised arithmetic is markedly faster than a loop)"],"avoid":["writing a Python loop to compute a column that could be expressed as vector arithmetic"],"reach":"For any derived numeric column where the formula is the same for every row.","example":"<pre>df[<span class=\"s\">\"margin\"</span>] = df[<span class=\"s\">\"reimbursement\"</span>] - df[<span class=\"s\">\"cost\"</span>]</pre>","tags":["transform","new","column","compute","derive","vectorize","spalte"]},{"id":"nlargest","code":"df.nlargest(10, \"cost\")","stage":"transform","short":"Return the n rows with the largest values in a column.","what":"Returns the top <em>n</em> rows ordered by the specified column. Faster than a full sort followed by <code>head</code>.","tokens":[[".nlargest(...)","Method returning top-n rows by a column."]],"requires":["a numeric column"],"important":["only the extremes are of interest"],"avoid":["calling on a categorical column"],"reach":"For top-N selection without sorting the entire table.","example":"<pre>df.nlargest(<span class=\"s\">10</span>, <span class=\"s\">\"cost\"</span>)\ndf.nsmallest(<span class=\"s\">5</span>, <span class=\"s\">\"margin\"</span>)</pre>","tags":["transform","top","largest","smallest","nlargest","nsmallest","rank"]},{"id":"np_select","code":"np.select(conditions, choices, default=...)","stage":"transform","short":"Multi-way conditional assignment.","what":"Returns an array containing the choice corresponding to the first true condition for each element. A default value is used where no condition is true.","tokens":[["np.select(...)","Function for multi-way conditional selection."],["conditions","A list of boolean arrays, evaluated in order."],["choices","A list of values corresponding to the conditions."],["default=...","Value used where no condition is true."]],"requires":["NumPy imported","matched lists of conditions and choices"],"important":["several mutually exclusive cases must be expressed"],"avoid":["overlapping conditions where the order accidentally determines the result"],"reach":"For multi-way conditional logic that would otherwise nest <code>np.where</code>.","example":"<pre>conds = [df[<span class=\"s\">\"age\"</span>] &lt; <span class=\"s\">18</span>,\n         df[<span class=\"s\">\"age\"</span>] &lt; <span class=\"s\">65</span>,\n         df[<span class=\"s\">\"age\"</span>] &gt;= <span class=\"s\">65</span>]\nlabels = [<span class=\"s\">\"child\"</span>, <span class=\"s\">\"adult\"</span>, <span class=\"s\">\"senior\"</span>]\ndf[<span class=\"s\">\"band\"</span>] = np.select(conds, labels, default=<span class=\"s\">\"unknown\"</span>)</pre>","tags":["transform","select","conditional","multi","if","np.select","mehrwertig"]},{"id":"np_where","code":"np.where(cond, a, b)","stage":"transform","short":"Two-way conditional assignment.","what":"Returns an array (or Series) containing <code>a</code> where <code>cond</code> is true and <code>b</code> where it is false. Vectorised, applies element by element.","tokens":[["np.where(...)","Function for element-wise conditional selection."],["cond","A boolean array or Series (typically derived from a comparison)."],["a","Value (or array) used where <code>cond</code> is true."],["b","Value (or array) used where <code>cond</code> is false."]],"requires":["NumPy imported (<code>import numpy as np</code>)","a boolean mask and two alternatives"],"important":["the new value depends on a binary distinction (above/below a threshold, in/not in a list)"],"avoid":["nesting <code>np.where</code> more than twice — use <code>np.select</code> for multi-way logic"],"reach":"For a two-way conditional assignment.","example":"<pre><span class=\"k\">import</span> numpy <span class=\"k\">as</span> np\ndf[<span class=\"s\">\"band\"</span>] = np.where(df[<span class=\"s\">\"age\"</span>] &gt;= <span class=\"s\">65</span>, <span class=\"s\">\"senior\"</span>, <span class=\"s\">\"adult\"</span>)</pre>","tags":["transform","where","conditional","if","binary","np.where","bedingt"]},{"id":"query","code":"df.query(\"age > 65 and ward == 'ICU'\")","stage":"transform","short":"Filter rows using a string expression.","what":"Returns a DataFrame filtered by the supplied expression, written as if it were ordinary Python syntax but evaluated within the DataFrame's column namespace.","tokens":[[".query('...')","Method filtering by a string expression."]],"requires":["column names that are valid Python identifiers (no spaces)"],"important":["the filter is long enough that a string expression is more readable than a boolean mask"],"avoid":["interpolating untrusted user input into the query string"],"reach":"For longer or more complex filters, where the readability of a string expression exceeds that of a chain of <code>&amp;</code> and <code>|</code>.","example":"<pre>df.query(<span class=\"s\">\"age &gt; 65 and ward == 'ICU'\"</span>)</pre>","tags":["transform","query","filter","string","subset","expression"]},{"id":"rename","code":"df.rename(columns={\"pid\":\"patient_id\"})","stage":"transform","short":"Rename columns or index labels.","what":"Returns a DataFrame with the specified columns renamed according to the mapping.","tokens":[[".rename(...)","Method renaming axis labels."],["columns={\"old\":\"new\"}","Mapping from current names to replacements."]],"requires":["a DataFrame with the named columns"],"important":["column names from an export are not consistent with conventions used elsewhere in the analysis"],"avoid":[],"reach":"To standardise column names across sources, particularly before a merge.","example":"<pre>df = df.rename(columns={<span class=\"s\">\"pid\"</span>: <span class=\"s\">\"patient_id\"</span>,\n                        <span class=\"s\">\"adm_dt\"</span>: <span class=\"s\">\"admission_date\"</span>})</pre>","tags":["transform","rename","columns","umbenennen"]},{"id":"sort_values","code":"df.sort_values(\"los_days\", ascending=False)","stage":"transform","short":"Sort the DataFrame by one or more columns.","what":"Returns a DataFrame whose rows are ordered according to the values of the supplied column(s).","tokens":[[".sort_values(...)","Method ordering rows."],["\"los_days\"","The column on which to sort."],["ascending=False","Reverses the default ascending order."]],"requires":["the column(s) supplied exist and are orderable"],"important":["the table is to be presented to a reader who locates rows by magnitude"],"avoid":["sorting the whole table when only the top-N is required (use <code>nlargest</code>)"],"reach":"To prepare a table for display or for selection by rank.","example":"<pre>df.sort_values([<span class=\"s\">\"ward\"</span>, <span class=\"s\">\"los_days\"</span>], ascending=[<span class=\"k\">True</span>, <span class=\"k\">False</span>])</pre>","tags":["transform","sort","order","values","rank","sortieren"]},{"id":"groupby_agg","code":"df.groupby(\"ward\").agg({\"los_days\":\"mean\", \"cost\":\"sum\"})","stage":"aggregate","short":"Compute different statistics for different columns.","what":"<code>agg</code> accepts a dictionary mapping each column to the statistic (or list of statistics) to compute. Returns a DataFrame of summaries.","tokens":[[".agg({...})","Method applying named aggregations per column."]],"requires":["a column to group by; columns and statistics for aggregation"],"important":["several different statistics are required in a single summary"],"avoid":[],"reach":"When the per-group summary involves more than one column or more than one statistic.","example":"<pre>summary = df.groupby(<span class=\"s\">\"ward\"</span>).agg(\n    avg_los = (<span class=\"s\">\"los_days\"</span>, <span class=\"s\">\"mean\"</span>),\n    total_cost = (<span class=\"s\">\"cost\"</span>, <span class=\"s\">\"sum\"</span>),\n    n_cases = (<span class=\"s\">\"patient_id\"</span>, <span class=\"s\">\"nunique\"</span>)\n)</pre>","tags":["aggregate","groupby","agg","multiple","summary","statistik"]},{"id":"groupby_apply","code":"df.groupby(\"ward\").apply(lambda g: g.nlargest(3, \"los_days\"))","stage":"aggregate","short":"Apply an arbitrary function to each group.","what":"Passes each group as a sub-DataFrame to the function and combines the returned objects. The most general — and slowest — groupby operation; reach for it only when the task is not expressible with <code>agg</code>, <code>transform</code> or <code>filter</code>, such as a per-group top-N.","tokens":[["df.groupby(\"ward\")","Partitions by ward."],[".apply(","Calls the function once per group and recombines the results."],["lambda g: g.nlargest(3, \"los_days\")","Per group: the three rows with the largest stay."]],"requires":["a grouping key","a function accepting a sub-DataFrame"],"important":["the per-group operation is not a simple reduction or broadcast (e.g. top-N per group)"],"avoid":["a plain reduction (use <code>agg</code>) or a group-aligned statistic (use <code>transform</code>) — both are far faster and clearer"],"reach":"When the per-group computation cannot be expressed by agg/transform/filter — used sparingly.","example":"<pre>top3 = (df.groupby(<span class=\"s\">\"ward\"</span>)\n          .apply(<span class=\"k\">lambda</span> g: g.nlargest(<span class=\"s\">3</span>, <span class=\"s\">\"los_days\"</span>))\n          .reset_index(drop=<span class=\"k\">True</span>))</pre>","tags":["aggregate","groupby","apply","per-group","top-n","anwenden"]},{"id":"groupby_count","code":"df.groupby(\"ward\")[\"los_days\"].count()","stage":"aggregate","short":"Number of non-missing values per group.","what":"Counts, within each group, the rows whose value in the selected column is not NaN. This is the crucial distinction from <code>size</code>: <code>count</code> excludes missing values and is computed per column; <code>size</code> counts every row in the group regardless of missingness.","tokens":[["df.groupby(\"ward\")","Partitions the rows by the distinct values of <code>ward</code>."],["[\"los_days\"]","Selects the column whose non-missing entries are counted."],[".count()","Counts non-missing values in that column per group."]],"requires":["a grouping key","a column on which non-missing entries are meaningful"],"important":["the number of valid observations per group is needed (e.g. a denominator)","missing values must be excluded from the tally"],"avoid":["the total number of rows per group is wanted including missing ones (use <code>size</code>)"],"reach":"When the count must reflect only valid observations per group, for instance as the denominator of a per-group rate.","example":"<pre>valid = df.groupby(<span class=\"s\">\"ward\"</span>)[<span class=\"s\">\"los_days\"</span>].count()</pre>","tags":["aggregate","groupby","count","size","non-missing","anzahl","denominator"]},{"id":"groupby_cumcount","code":"df.groupby(\"patient_id\").cumcount()","stage":"aggregate","short":"Running 0-based index of each row within its group.","what":"Returns a Series, the same length as the DataFrame, numbering the rows within each group 0, 1, 2, … in current order. Useful for sequencing repeated events per entity — for instance numbering a patient's successive encounters.","tokens":[["df.groupby(\"patient_id\")","Partitions by patient."],[".cumcount()","Numbers the rows within each group 0,1,2,… in order."]],"requires":["a grouping key","a meaningful row order (sort beforehand)"],"important":["each repeated event per entity must be sequenced (visit number, attempt number)"],"avoid":["the row order is arbitrary — the sequence is then meaningless"],"reach":"When successive rows belonging to the same entity must be numbered, such as the n-th encounter of each patient.","example":"<pre>df[<span class=\"s\">\"visit_no\"</span>] = (df.sort_values(<span class=\"s\">\"date\"</span>)\n                  .groupby(<span class=\"s\">\"patient_id\"</span>).cumcount() + <span class=\"s\">1</span>)</pre>","tags":["aggregate","groupby","cumcount","sequence","visit number","laufende nummer"]},{"id":"groupby_filter","code":"df.groupby(\"ward\").filter(lambda g: len(g) >= 10)","stage":"aggregate","short":"Keep only the rows of groups satisfying a condition.","what":"Returns the original rows, restricted to those belonging to groups for which the predicate is True. The predicate receives each group as a sub-DataFrame and must return a single boolean. Unlike <code>agg</code>, the result has the same columns as the input — it filters rows, it does not summarise.","tokens":[["df.groupby(\"ward\")","Partitions by ward."],[".filter(","Keeps or discards entire groups by a per-group test."],["lambda g: len(g) >= 10","The test: keep groups with at least ten rows."]],"requires":["a grouping key","a predicate returning one boolean per group"],"important":["groups that are too small (or otherwise unqualified) should be removed before analysis"],"avoid":["a per-row condition is intended — that is ordinary boolean indexing, not <code>groupby.filter</code>"],"reach":"When whole groups must be excluded by a group-level criterion, such as dropping wards with too few cases.","example":"<pre>big = df.groupby(<span class=\"s\">\"ward\"</span>).filter(<span class=\"k\">lambda</span> g: <span class=\"k\">len</span>(g) &gt;= <span class=\"s\">10</span>)</pre>","tags":["aggregate","groupby","filter","small groups","predicate","gruppen filtern"]},{"id":"groupby_head","code":"df.groupby(\"ward\").head(2)","stage":"aggregate","short":"First n rows of each group.","what":"Returns the first n rows from each group, in the DataFrame's current order. Sort the DataFrame meaningfully first (for example by date) so that \"first\" carries the intended meaning, such as the earliest encounters per ward.","tokens":[["df.groupby(\"ward\")","Partitions by ward."],[".head(2)","Takes the first two rows of each group, in current order."]],"requires":["a grouping key","a meaningful row order (sort beforehand)"],"important":["a sample of each group is wanted, or the earliest/first records per group after sorting"],"avoid":["the row order is arbitrary — \"first\" is then undefined"],"reach":"When the leading rows of every group are required, typically the earliest records per group after sorting by time.","example":"<pre>first2 = df.sort_values(<span class=\"s\">\"date\"</span>).groupby(<span class=\"s\">\"ward\"</span>).head(<span class=\"s\">2</span>)</pre>","tags":["aggregate","groupby","head","first","per-group","erste"]},{"id":"groupby_mean","code":"df.groupby(\"ward\")[\"los_days\"].mean()","stage":"aggregate","short":"Compute the mean of a column within each group.","what":"Splits the DataFrame by the values in <code>ward</code>, takes the column <code>los_days</code> within each group, and computes its mean.","tokens":[[".groupby(\"ward\")","Partitions the DataFrame by unique values of <code>ward</code>."],["[\"los_days\"]","Selects a single column within each partition."],[".mean()","Computes the mean per group."]],"requires":["a column to group by and a numeric column to aggregate"],"important":["a summary statistic is required for each level of a categorical variable"],"avoid":["forgetting <code>.reset_index()</code> when a flat DataFrame is needed for downstream use"],"reach":"For any per-group summary of a single statistic.","example":"<pre>df.groupby(<span class=\"s\">\"ward\"</span>)[<span class=\"s\">\"los_days\"</span>].mean()</pre>","tags":["aggregate","groupby","group","mean","average","summary","durchschnitt"]},{"id":"groupby_multikey","code":"df.groupby([\"ward\", \"gender\"])[\"los_days\"].mean()","stage":"aggregate","short":"Group by more than one key.","what":"Passing a list of columns groups by their combination: one group per observed tuple of values. The result has a MultiIndex whose levels are the grouping columns; <code>reset_index()</code> flattens it back to ordinary columns for further use.","tokens":[["df.groupby([...])","Partitions by the combination of the listed columns."],["[\"ward\", \"gender\"]","The grouping keys; one group per (ward, gender) pair that occurs."],["[\"los_days\"].mean()","Aggregates the chosen column within each combined group."]],"requires":["two or more grouping columns","an aggregation appropriate to the selected column"],"important":["the question concerns a cross-classification (per ward and per gender)","the result will be reshaped — follow with <code>reset_index</code> or <code>unstack</code>"],"avoid":["grouping by many high-cardinality keys, which yields sparse, tiny groups"],"reach":"When the summary must be broken down by a combination of categories rather than a single one.","example":"<pre>g = (df.groupby([<span class=\"s\">\"ward\"</span>, <span class=\"s\">\"gender\"</span>])[<span class=\"s\">\"los_days\"</span>]\n       .mean().reset_index())</pre>","tags":["aggregate","groupby","multiple keys","multiindex","cross","gruppieren"]},{"id":"groupby_nth","code":"df.groupby(\"ward\").nth(0)","stage":"aggregate","short":"The n-th row of each group by position.","what":"Selects the row at the given positional index within each group (0 is the first, -1 the last). Order-dependent like <code>head</code>. Commonly used after sorting by time to pick the first or most recent record per entity.","tokens":[["df.groupby(\"ward\")","Partitions by ward."],[".nth(0)","Selects the first row (position 0) of each group; -1 would be the last."]],"requires":["a grouping key","a meaningful row order"],"important":["exactly one positional record per group is wanted (the first or the last)"],"avoid":["the row order is arbitrary","several rows per group are needed (use <code>head</code>)"],"reach":"When a single positional record per group is required, such as each patient's first or latest encounter.","example":"<pre>latest = df.sort_values(<span class=\"s\">\"date\"</span>).groupby(<span class=\"s\">\"patient_id\"</span>).nth(<span class=\"s\">-1</span>)</pre>","tags":["aggregate","groupby","nth","first","last","position","n-te"]},{"id":"groupby_size","code":"df.groupby(\"ward\").size()","stage":"aggregate","short":"Count the number of rows in each group.","what":"Returns a Series mapping each group to its number of rows.","tokens":[[".size()","Method returning row counts per group."]],"requires":["a column to group by"],"important":["one wishes to know how many observations underlie each group's summary"],"avoid":["confusing <code>size</code> with <code>count</code>: <code>count</code> ignores <code>NaN</code> per column; <code>size</code> counts all rows in the group"],"reach":"To accompany any per-group summary with an explicit count.","example":"<pre>df.groupby(<span class=\"s\">\"ward\"</span>).size()</pre>","tags":["aggregate","size","count","groupby","frequency","anzahl"]},{"id":"named_aggregation","code":"df.groupby(\"ward\").agg(avg_los=(\"los_days\", \"mean\"), n=(\"id\", \"count\"))","stage":"aggregate","short":"Named aggregation: control the output column names.","what":"The keyword form of <code>agg</code> maps each desired output column name to a (source column, function) pair. It produces flat, explicitly named columns, avoiding the awkward MultiIndex that the list form of <code>agg</code> creates, and is the recommended modern style.","tokens":[["df.groupby(\"ward\")","Partitions by ward."],[".agg(","Aggregates each group, naming the outputs explicitly."],["avg_los=(\"los_days\", \"mean\")","Output column <code>avg_los</code> = mean of <code>los_days</code>."],["n=(\"id\", \"count\")","Output column <code>n</code> = count of <code>id</code> per group."]],"requires":["a grouping key","for each output: a source column and an aggregation function"],"important":["several statistics of possibly different columns are needed with clear names","the result feeds a report or a further merge"],"avoid":["the list form <code>agg(['mean','sum'])</code> when flat named columns are preferable"],"reach":"When a per-group summary needs several explicitly named statistics, ready for reporting or joining.","example":"<pre>summary = df.groupby(<span class=\"s\">\"ward\"</span>).agg(\n    avg_los=(<span class=\"s\">\"los_days\"</span>, <span class=\"s\">\"mean\"</span>),\n    n=(<span class=\"s\">\"id\"</span>, <span class=\"s\">\"count\"</span>))</pre>","tags":["aggregate","groupby","agg","named aggregation","summary","benannt"]},{"id":"pivot_table","code":"pd.pivot_table(df, index=\"ward\", columns=\"month\", values=\"los_days\", aggfunc=\"mean\")","stage":"aggregate","short":"Produce a wide-format summary indexed by two variables.","what":"Creates a table whose rows are the values of <code>index</code>, columns are the values of <code>columns</code>, and cells contain the chosen aggregation of <code>values</code>.","tokens":[["pd.pivot_table(...)","Function producing a wide summary."],["index=\"ward\"","Variable forming the rows."],["columns=\"month\"","Variable forming the columns."],["values=\"los_days\"","Variable to aggregate."],["aggfunc=\"mean\"","Aggregation function."]],"requires":["a DataFrame with the named columns"],"important":["the summary is to be presented to a reader rather than processed further"],"avoid":["pivoting on a high-cardinality column (produces an unmanageably wide table)"],"reach":"For a presentation-ready summary indexed by two categorical variables.","example":"<pre>pd.pivot_table(df, index=<span class=\"s\">\"ward\"</span>, columns=<span class=\"s\">\"month\"</span>,\n               values=<span class=\"s\">\"los_days\"</span>, aggfunc=<span class=\"s\">\"mean\"</span>)</pre>","tags":["aggregate","pivot","pivot_table","wide","crosstab","summary"]},{"id":"transform","code":"df.groupby(\"ward\")[\"los_days\"].transform(\"mean\")","stage":"aggregate","short":"Broadcast a group-level statistic back to every row.","what":"Returns a Series of the same length as the original DataFrame, in which each row carries the statistic computed for its group.","tokens":[[".transform('mean')","Computes the group mean and assigns it to every row of the group."]],"requires":["a column to group by"],"important":["a deviation from the group mean (or other per-group reference) is required"],"avoid":["confusing <code>transform</code> with <code>agg</code>: <code>agg</code> reduces; <code>transform</code> broadcasts"],"reach":"When a per-row variable depends on a per-group statistic (e.g. <em>deviation from ward mean</em>).","example":"<pre>df[<span class=\"s\">\"ward_avg_los\"</span>] = df.groupby(<span class=\"s\">\"ward\"</span>)[<span class=\"s\">\"los_days\"</span>].transform(<span class=\"s\">\"mean\"</span>)\ndf[<span class=\"s\">\"los_above_ward_avg\"</span>] = df[<span class=\"s\">\"los_days\"</span>] - df[<span class=\"s\">\"ward_avg_los\"</span>]</pre>","tags":["aggregate","transform","broadcast","groupby","per-row"]},{"id":"combine_first","code":"a.combine_first(b)","stage":"merge","short":"Use one DataFrame to fill in the gaps of another.","what":"Cell by cell: where <code>a</code> has a value, that value is retained; where <code>a</code> is <code>NaN</code>, the corresponding value from <code>b</code> is used. The two DataFrames are aligned on index and columns.","tokens":[[".combine_first(b)","Method using <code>b</code> as a fallback source for missing cells in <code>a</code>."]],"requires":["two DataFrames describing the same entities, aligned on index"],"important":["<code>a</code> is the primary source but contains gaps that <code>b</code> can supply"],"avoid":["confusing with <code>merge</code> — <code>combine_first</code> operates cell-by-cell on aligned tables, not key-by-key"],"reach":"When two sources describe the same entities and one is to be used as a fallback for the other.","example":"<pre>combined = primary.combine_first(fallback)\ncombined.isna().sum()   <span class=\"c\"># compare before and after</span></pre>","tags":["merge","combine_first","complement","fill","fallback","luecke fuellen"]},{"id":"concat","code":"pd.concat([a, b], ignore_index=True)","stage":"merge","short":"Stack two DataFrames vertically or side by side.","what":"Combines DataFrames along an axis. <code>axis=0</code> stacks rows (default); <code>axis=1</code> stacks columns. <code>ignore_index=True</code> renumbers the rows of the result.","tokens":[["pd.concat([...], ...)","Function concatenating DataFrames."],["[a, b]","The DataFrames to combine."],["ignore_index=True","Discards original indices and renumbers."]],"requires":["DataFrames whose columns align (for <code>axis=0</code>) or whose indices align (for <code>axis=1</code>)"],"important":["the two sources represent the same kind of records (e.g. successive months)"],"avoid":["concatenating without checking that the columns match"],"reach":"For combining records of the same shape from different periods or sources.","example":"<pre>full_year = pd.concat([q1, q2, q3, q4], ignore_index=<span class=\"k\">True</span>)</pre>","tags":["merge","concat","stack","append","union","stapeln"]},{"id":"merge","code":"a.merge(b, on=\"patient_id\", how=\"left\")","stage":"merge","short":"Join two DataFrames on a shared key.","what":"Combines two DataFrames row-by-row by matching values in a key column. <code>how</code> determines which rows are retained when a key is missing on one side.","tokens":[[".merge(b, ...)","Method joining the calling DataFrame with another."],["on=\"patient_id\"","The column present in both tables, used as the join key."],["how=\"left\"","Retains all rows of the calling DataFrame; rows of <code>b</code> without a match are dropped."]],"requires":["two DataFrames sharing a key column"],"important":["the cardinality of the key is asserted (<code>validate='1:m'</code>) to catch unintended fan-out","an <code>indicator</code> column is added during debugging to audit the match outcome"],"avoid":["merging on a column whose name differs between tables without <code>left_on</code> / <code>right_on</code>","merging with duplicate keys on both sides — the result grows multiplicatively"],"reach":"Whenever information from two tables must be combined along a shared identifier.","example":"<pre>full = encounters.merge(patients, on=<span class=\"s\">\"patient_id\"</span>, how=<span class=\"s\">\"left\"</span>,\n                        validate=<span class=\"s\">\"m:1\"</span>, indicator=<span class=\"k\">True</span>)\nfull[<span class=\"s\">\"_merge\"</span>].value_counts()</pre>","tags":["merge","join","key","inner","left","right","outer","verknuepfen"]},{"id":"np_column_stack","code":"np.column_stack([a, b])","stage":"merge","short":"Treat several 1D arrays as columns of a 2D result.","what":"Combines 1D arrays into a 2D array, with each input becoming one column.","tokens":[["np.column_stack([...])","Function forming a 2D array from 1D columns."]],"requires":["1D arrays of the same length"],"important":["one wishes to build a table column by column from separate vectors"],"avoid":[],"reach":"When several 1D arrays of equal length should become the columns of a 2D table.","example":"<pre>ages   = np.array([<span class=\"s\">34</span>, <span class=\"s\">58</span>, <span class=\"s\">72</span>])\ncosts  = np.array([<span class=\"s\">450</span>, <span class=\"s\">1200</span>, <span class=\"s\">2300</span>])\nnp.column_stack([ages, costs])   <span class=\"c\"># shape (3, 2)</span></pre>","tags":["merge","numpy","column_stack","columns","1d","2d","table"]},{"id":"np_concatenate","code":"np.concatenate([a, b], axis=0)","stage":"merge","short":"Concatenate NumPy arrays along an existing axis.","what":"Joins arrays end-to-end along the specified axis. For 1D arrays, the result is the two arrays placed in sequence; for 2D, <code>axis=0</code> stacks rows, <code>axis=1</code> stacks columns.","tokens":[["np.concatenate([...], axis=...)","Function joining arrays along an existing dimension."],["axis=0","Concatenate along rows."],["axis=1","Concatenate along columns."]],"requires":["arrays whose shapes match on all axes other than the concatenation axis"],"important":["combining batches of data with the same number of features"],"avoid":["concatenating arrays of incompatible shape — the error message is sometimes terse"],"reach":"For combining NumPy arrays where pandas would be unnecessary overhead.","example":"<pre><span class=\"k\">import</span> numpy <span class=\"k\">as</span> np\nnp.concatenate([a, b])             <span class=\"c\"># 1D: end to end</span>\nnp.concatenate([A, B], axis=<span class=\"s\">0</span>)   <span class=\"c\"># 2D: more rows</span>\nnp.concatenate([A, B], axis=<span class=\"s\">1</span>)   <span class=\"c\"># 2D: more columns</span></pre>","tags":["merge","numpy","concatenate","array","1d","2d","axis","verbinden"]},{"id":"np_hstack","code":"np.hstack([a, b])","stage":"merge","short":"Stack arrays horizontally (side by side).","what":"Places the inputs side by side. For 1D inputs, the result is end-to-end (equivalent to <code>concatenate</code>); for 2D inputs, the arrays are joined column-wise.","tokens":[["np.hstack([...])","Function stacking arrays horizontally."]],"requires":["arrays with the same number of rows (or 1D arrays)"],"important":["one wishes to extend a 2D array with additional columns"],"avoid":[],"reach":"For horizontal stacking — additional columns onto a 2D array, or end-to-end for 1D.","example":"<pre>np.hstack([A, B])</pre>","tags":["merge","numpy","hstack","stack","horizontal","columns"]},{"id":"np_vstack","code":"np.vstack([a, b])","stage":"merge","short":"Stack arrays vertically (as further rows).","what":"Places the inputs one above the other. For 1D inputs, each becomes a row of the resulting 2D array.","tokens":[["np.vstack([...])","Function stacking arrays vertically."]],"requires":["arrays with the same number of columns (or 1D arrays of the same length)"],"important":["one wishes to produce a 2D result from 1D inputs"],"avoid":[],"reach":"For vertical stacking of NumPy arrays; works uniformly for 1D and 2D inputs.","example":"<pre>np.vstack([a, b])   <span class=\"c\"># 1D + 1D → 2D shape (2, n)</span></pre>","tags":["merge","numpy","vstack","stack","vertical","rows"]},{"id":"ax_annotate","code":"ax.annotate(\"peak\", xy=(x, y), xytext=(x, y+5), arrowprops={})","stage":"visualize","short":"Add an explanatory annotation, optionally with an arrow.","what":"Places text at <code>xytext</code> pointing at the data location <code>xy</code>, with an optional arrow. Used to draw the reader's attention to a specific feature of the plot — a peak, an outlier, an event.","tokens":[["ax.annotate(","Adds an annotation anchored to a data point."],["\"peak\"","The annotation text."],["xy=(x, y)","The data point being annotated."],["xytext=(x, y+5)","Where the text is placed."],["arrowprops={}","If given, draws an arrow from text to point."]],"requires":["a Matplotlib axes","the data coordinates of the feature"],"important":["a specific feature of the figure must be called out for the reader"],"avoid":["cluttering the plot with many annotations"],"reach":"When a particular point of a figure needs an explanatory callout.","example":"<pre>ax.annotate(<span class=\"s\">\"outbreak\"</span>, xy=(<span class=\"s\">d</span>, <span class=\"s\">peak</span>),\n            xytext=(<span class=\"s\">d</span>, peak*<span class=\"s\">1.2</span>), arrowprops={<span class=\"s\">\"arrowstyle\"</span>: <span class=\"s\">\"-&gt;\"</span>})</pre>","tags":["visualize","annotate","annotation","arrow","callout","beschriftung"]},{"id":"ax_bar","code":"ax.bar(x, y)","stage":"visualize","short":"Draw a bar chart.","what":"Draws bars whose heights are the values <code>y</code> at positions <code>x</code>. For categorical <code>x</code>, the positions are the categories themselves.","tokens":[["ax.bar(...)","Method drawing bars on the axes <code>ax</code>."],["x","Positions or category labels."],["y","Heights."]],"requires":["an axes object and matching <code>x</code> and <code>y</code> arrays"],"important":["the question concerns comparison of a single number across categories"],"avoid":["using bars for continuous time series — prefer a line plot"],"reach":"For comparison of a single quantity across a small number of categories.","example":"<pre>ax.bar(df[<span class=\"s\">\"ward\"</span>], df[<span class=\"s\">\"occupancy\"</span>])\nax.set_title(<span class=\"s\">\"Bed occupancy by ward\"</span>)</pre>","tags":["visualize","bar","barchart","matplotlib","ax.bar","balkendiagramm"]},{"id":"ax_boxplot","code":"ax.boxplot([a, b, c], labels=[...])","stage":"visualize","short":"Box plot comparing distributions across groups.","what":"Draws, for each supplied array, a box from the first to the third quartile with the median marked and whiskers to the data extremes (outliers as points). The standard way to compare the distribution and spread of a numeric variable across a few groups.","tokens":[["ax.boxplot(","Draws box-and-whisker summaries."],["[a, b, c]","A list of arrays, one box per group."],["labels=[...]","The category label under each box."]],"requires":["a Matplotlib axes","the values split into one array per group"],"important":["the spread and skew of a numeric variable must be compared across a small number of groups"],"avoid":["very many groups (the figure becomes unreadable)","presenting a box plot to an audience unfamiliar with quartiles without explanation"],"reach":"When distributions of a numeric variable must be compared across categories, including their spread and outliers.","example":"<pre>groups = [g[<span class=\"s\">\"los_days\"</span>].values <span class=\"k\">for</span> _, g <span class=\"k\">in</span> df.groupby(<span class=\"s\">\"ward\"</span>)]\nax.boxplot(groups)</pre>","tags":["visualize","boxplot","distribution","quartile","outlier","boxplot"]},{"id":"ax_errorbar","code":"ax.errorbar(x, y, yerr=err, fmt='o')","stage":"visualize","short":"Points or line with error bars.","what":"Plots y against x with vertical (and/or horizontal) error bars of length <code>yerr</code>. Used to display a mean together with its uncertainty (standard deviation, standard error, or confidence interval).","tokens":[["ax.errorbar(","Plots data with error bars."],["x, y","The point coordinates (e.g. category and mean)."],["yerr=err","Half-length of each vertical error bar."],["fmt='o'","Marker style; 'o' shows points without a connecting line."]],"requires":["a Matplotlib axes","an uncertainty value per point"],"important":["a summary statistic must be shown together with its uncertainty"],"avoid":["plotting error bars whose statistical meaning (SD vs SE vs CI) is not stated"],"reach":"When estimates must be presented with their uncertainty, such as group means with confidence intervals.","example":"<pre>m = df.groupby(<span class=\"s\">\"ward\"</span>)[<span class=\"s\">\"los_days\"</span>].mean()\ns = df.groupby(<span class=\"s\">\"ward\"</span>)[<span class=\"s\">\"los_days\"</span>].std()\nax.errorbar(m.index, m, yerr=s, fmt=<span class=\"s\">\"o\"</span>)</pre>","tags":["visualize","errorbar","uncertainty","confidence","fehlerbalken"]},{"id":"ax_fill_between","code":"ax.fill_between(x, lo, hi, alpha=0.2)","stage":"visualize","short":"Shade a band between two curves.","what":"Fills the area between the lower and upper curves across x. Most often used behind a line to depict a confidence or prediction band.","tokens":[["ax.fill_between(","Shades the region between two y-series."],["x","Shared x coordinates."],["lo, hi","Lower and upper bounds of the band."],["alpha=0.2","Transparency so an overlaid line stays visible."]],"requires":["a Matplotlib axes","aligned lower and upper bound arrays"],"important":["a confidence or prediction interval must be shown around a line"],"avoid":["an opaque fill that hides the central line"],"reach":"When a band of uncertainty must accompany a line, such as a confidence interval around a trend.","example":"<pre>ax.plot(x, mean)\nax.fill_between(x, mean - sd, mean + sd, alpha=<span class=\"s\">0.2</span>)</pre>","tags":["visualize","fill_between","band","confidence","interval","fläche"]},{"id":"ax_hist","code":"ax.hist(values, bins=30)","stage":"visualize","short":"Draw a histogram.","what":"Bins the values and draws a bar for each bin whose height is the count of values in the bin.","tokens":[["ax.hist(...)","Method drawing a histogram."],["values","The data."],["bins=30","Number of bins (or explicit bin edges)."]],"requires":["a numeric array or Series"],"important":["the question concerns the distribution of a single continuous variable"],"avoid":["accepting the default bin count without inspecting the result"],"reach":"For the distribution of a single continuous variable.","example":"<pre>ax.hist(df[<span class=\"s\">\"los_days\"</span>], bins=<span class=\"s\">30</span>)\nax.set_xlabel(<span class=\"s\">\"Length of stay (days)\"</span>)</pre>","tags":["visualize","hist","histogram","distribution","matplotlib","verteilung"]},{"id":"ax_plot","code":"ax.plot(x, y)","stage":"visualize","short":"Draw a line plot.","what":"Connects successive (x, y) points with line segments.","tokens":[["ax.plot(...)","Method drawing a line plot."]],"requires":["paired arrays of equal length"],"important":["the question concerns evolution over an ordered variable, typically time"],"avoid":["using line plots for unordered categories"],"reach":"For trends over an ordered or continuous variable.","example":"<pre>ax.plot(monthly.index, monthly[<span class=\"s\">\"admissions\"</span>])\nax.set_xlabel(<span class=\"s\">\"Month\"</span>); ax.set_ylabel(<span class=\"s\">\"Admissions\"</span>)</pre>","tags":["visualize","line","plot","trend","time","matplotlib","liniendiagramm"]},{"id":"ax_scatter","code":"ax.scatter(df[\"x\"], df[\"y\"])","stage":"visualize","short":"Scatter plot of two numeric variables.","what":"Draws one point per observation at (x, y). The standard display for the relationship between two continuous variables. <code>c=</code> colours points by a third variable, <code>s=</code> sizes them, <code>alpha=</code> reveals density where points overlap.","tokens":[["ax.scatter(","Plots individual points on the axes."],["df[\"x\"], df[\"y\"]","The x and y coordinates of each point."]],"requires":["a Matplotlib axes (<code>fig, ax = plt.subplots()</code>)","two aligned numeric Series"],"important":["the relationship or correlation between two continuous variables is the question","overplotting is likely — set <code>alpha</code> below 1"],"avoid":["one axis is categorical (use a bar or box plot)","tens of thousands of points without transparency or sampling"],"reach":"When the association between two continuous variables must be shown point by point.","example":"<pre>fig, ax = plt.subplots()\nax.scatter(df[<span class=\"s\">\"age\"</span>], df[<span class=\"s\">\"cost_eur\"</span>], alpha=<span class=\"s\">0.4</span>)</pre>","tags":["visualize","scatter","points","correlation","streudiagramm"]},{"id":"ax_set","code":"ax.set_title(\"...\"); ax.set_xlabel(\"...\"); ax.set_ylabel(\"...\")","stage":"visualize","short":"Label the axes and the plot.","what":"Three methods setting the title and axis labels. A plot intended for any external reader requires all three.","tokens":[["ax.set_title('...')","Sets the plot title."],["ax.set_xlabel('...')","Sets the label of the x-axis."],["ax.set_ylabel('...')","Sets the label of the y-axis."]],"requires":["an axes object"],"important":["always, on any plot intended to be read by anyone other than the author at the moment of writing"],"avoid":["omitting axis units — they are part of the label"],"reach":"After every plotting call, before saving or showing.","example":"<pre>ax.set_title(<span class=\"s\">\"Bed occupancy by ward — Q1 2026\"</span>)\nax.set_xlabel(<span class=\"s\">\"Ward\"</span>)\nax.set_ylabel(<span class=\"s\">\"Occupancy rate (%)\"</span>)</pre>","tags":["visualize","title","label","axis","matplotlib","beschriftung"]},{"id":"ax_text","code":"ax.text(x, y, \"label\")","stage":"visualize","short":"Place free text at data coordinates.","what":"Writes a string at the data position (x, y) — used for value labels on bars, or a note inside the plotting area. Simpler than <code>annotate</code>: no arrow, no separate text position.","tokens":[["ax.text(","Places text at a data coordinate."],["x, y","The position in data units."],["\"label\"","The string to display."]],"requires":["a Matplotlib axes","the data coordinates for the text"],"important":["bars or points should carry their value as a label"],"avoid":["positioning in axes-fraction coordinates is needed (use <code>transform=ax.transAxes</code>)"],"reach":"When values or short notes must be written directly onto the plot.","example":"<pre><span class=\"k\">for</span> i, v <span class=\"k\">in</span> <span class=\"k\">enumerate</span>(vals):\n    ax.text(i, v + <span class=\"s\">0.5</span>, <span class=\"k\">str</span>(v), ha=<span class=\"s\">\"center\"</span>)</pre>","tags":["visualize","text","label","value label","beschriftung"]},{"id":"ax_twinx","code":"ax2 = ax.twinx()","stage":"visualize","short":"A second y-axis sharing the same x-axis.","what":"Creates a twinned axes sharing the x-axis but with an independent y-axis on the right, so two series on different scales can be drawn together. Use sparingly and label both axes clearly — dual-axis charts are easy to read incorrectly.","tokens":[["ax2 = ax.twinx()","Creates a right-hand y-axis sharing the original x-axis."]],"requires":["an existing axes","two series that genuinely require different scales"],"important":["two related series on incomparable scales must share one time axis"],"avoid":["using a second axis where the series could share one scale — it invites misreading","leaving either y-axis unlabelled"],"reach":"When two series on different units must be compared over the same x-axis, with explicit labelling.","example":"<pre>ax.plot(t, admissions)\nax2 = ax.twinx()\nax2.plot(t, cost, color=<span class=\"s\">\"C1\"</span>)</pre>","tags":["visualize","twinx","secondary axis","dual axis","zweite achse"]},{"id":"savefig","code":"plt.savefig(\"fig.png\", dpi=200, bbox_inches=\"tight\")","stage":"visualize","short":"Save the current figure to a file.","what":"Writes the figure to disk in the format implied by the file extension. <code>dpi</code> controls raster resolution; <code>bbox_inches='tight'</code> trims excess whitespace.","tokens":[["plt.savefig(...)","Function saving the current figure."],["\"fig.png\"","File path; extension determines format."],["dpi=200","Resolution in dots per inch."],["bbox_inches=\"tight\"","Crops surrounding whitespace."]],"requires":["a figure produced by previous plotting calls"],"important":["called <em>before</em> <code>plt.show()</code> — calling after may save an empty canvas"],"avoid":["saving without <code>bbox_inches='tight'</code> for figures with rotated tick labels"],"reach":"Whenever a figure is to leave the notebook.","example":"<pre>plt.tight_layout()\nplt.savefig(<span class=\"s\">\"figures/occupancy.png\"</span>, dpi=<span class=\"s\">200</span>, bbox_inches=<span class=\"s\">\"tight\"</span>)\nplt.show()</pre>","tags":["visualize","save","savefig","export","figure","png","pdf"]},{"id":"sns_boxplot","code":"sns.boxplot(data=df, x=\"ward\", y=\"los_days\")","stage":"visualize","short":"Box plot by category (seaborn, tidy input).","what":"Draws a box plot of <code>y</code> grouped by <code>x</code> directly from a tidy DataFrame — no manual splitting into arrays. <code>hue=</code> adds a second categorical split. The concise seaborn counterpart of <code>ax.boxplot</code>.","tokens":[["sns.boxplot(","Box plot from tidy data."],["data=df","The tidy DataFrame."],["x=\"ward\"","Categorical variable defining the groups."],["y=\"los_days\"","Numeric variable summarised per group."]],"requires":["seaborn imported","tidy (long) data with a category and a numeric column"],"important":["distributions across categories must be compared with minimal code","a second split is needed (use <code>hue=</code>)"],"avoid":["wide-format data (melt it to long first)"],"reach":"When comparing a numeric distribution across categories straight from a tidy DataFrame.","example":"<pre>sns.boxplot(data=df, x=<span class=\"s\">\"ward\"</span>, y=<span class=\"s\">\"los_days\"</span>)</pre>","tags":["visualize","seaborn","boxplot","distribution","tidy","boxplot"]},{"id":"sns_heatmap","code":"sns.heatmap(corr, annot=True, cmap='coolwarm')","stage":"visualize","short":"Colour-encoded matrix (seaborn).","what":"Renders a 2-D matrix as a grid of coloured cells. The usual display for a correlation matrix or any pivoted table of magnitudes. <code>annot=True</code> overlays the numeric values; a diverging colormap suits data centred on zero.","tokens":[["sns.heatmap(","Draws a matrix as coloured cells."],["corr","The 2-D matrix (often the output of <code>df.corr()</code>)."],["annot=True","Writes each cell's value on it."],["cmap='coolwarm'","A diverging colormap for values around zero."]],"requires":["seaborn imported (<code>import seaborn as sns</code>)","a 2-D numeric matrix or DataFrame"],"important":["a correlation matrix or a small pivot table must be displayed visually"],"avoid":["a very large matrix with annotations (unreadable)","a sequential colormap for diverging data centred on zero"],"reach":"When a matrix of magnitudes — typically a correlation matrix — must be shown at a glance.","example":"<pre>sns.heatmap(df.corr(numeric_only=<span class=\"k\">True</span>), annot=<span class=\"k\">True</span>, cmap=<span class=\"s\">\"coolwarm\"</span>)</pre>","tags":["visualize","seaborn","heatmap","correlation","matrix","hitzekarte"]},{"id":"sns_regplot","code":"sns.regplot(data=df, x=\"age\", y=\"cost_eur\")","stage":"visualize","short":"Scatter with a fitted regression line (seaborn).","what":"Draws a scatter of y against x and overlays an ordinary-least-squares fit with a confidence band. A quick visual check of a linear relationship; it does not return model coefficients.","tokens":[["sns.regplot(","Scatter plus fitted regression line."],["data=df","The DataFrame."],["x=\"age\", y=\"cost_eur\"","The predictor and response columns."]],"requires":["seaborn imported","two numeric columns"],"important":["a linear trend between two variables must be assessed visually"],"avoid":["treating it as a model fit — it shows a line but returns no coefficients","a clearly non-linear relationship without <code>order=</code> or <code>lowess=True</code>"],"reach":"When a linear relationship between two numeric variables must be inspected quickly.","example":"<pre>sns.regplot(data=df, x=<span class=\"s\">\"age\"</span>, y=<span class=\"s\">\"cost_eur\"</span>)</pre>","tags":["visualize","seaborn","regplot","regression","trend","scatter"]},{"id":"subplots","code":"fig, ax = plt.subplots(figsize=(10,6))","stage":"visualize","short":"Create a figure and a set of axes.","what":"Initialises a Matplotlib figure (the canvas) and one or more axes (the coordinate systems on which to draw). The figure is the object eventually saved; the axes carry the drawing commands.","tokens":[["plt.subplots(...)","Function returning a figure and axes."],["figsize=(10,6)","Width and height of the figure in inches."],["fig, ax","Unpacks the two returned objects."]],"requires":["<code>import matplotlib.pyplot as plt</code>"],"important":["every plot intended for external use should begin with this call (object-oriented interface)"],"avoid":["mixing <code>plt.</code>-style and <code>ax.</code>-style calls in the same plot"],"reach":"As the first line of any plot beyond a quick exploratory view.","example":"<pre><span class=\"k\">import</span> matplotlib.pyplot <span class=\"k\">as</span> plt\nfig, ax = plt.subplots(figsize=(<span class=\"s\">10</span>, <span class=\"s\">6</span>))</pre>","tags":["visualize","matplotlib","figure","axes","subplots","plot"]},{"id":"to_csv","code":"df.to_csv(\"out.csv\", index=False)","stage":"export","short":"Write the DataFrame to a CSV file.","what":"Writes the DataFrame as comma-separated text. <code>index=False</code> suppresses writing the row index as an additional column.","tokens":[[".to_csv(...)","Method writing to CSV."],["\"out.csv\"","Destination path."],["index=False","Omits the row index from the output."]],"requires":["a DataFrame"],"important":["recipient is a human or a tool that reads CSV","UTF-8 encoding suffices, or a specific encoding is given"],"avoid":["writing the index when the default <code>RangeIndex</code> is uninformative","using comma as separator when the data themselves contain commas (use a different <code>sep=</code>)"],"reach":"For data delivery to a human reader or to a downstream non-Python tool.","example":"<pre>df.to_csv(<span class=\"s\">\"output/summary.csv\"</span>, index=<span class=\"k\">False</span>, encoding=<span class=\"s\">\"utf-8\"</span>)</pre>","tags":["export","csv","write","save","output","schreiben"]},{"id":"to_excel","code":"df.to_excel(\"out.xlsx\", sheet_name=\"Q1\", index=False)","stage":"export","short":"Write the DataFrame to an Excel sheet.","what":"Writes the DataFrame to an Excel workbook. Requires <code>openpyxl</code>.","tokens":[[".to_excel(...)","Method writing to Excel."],["\"out.xlsx\"","Destination path."],["sheet_name=\"Q1\"","Name of the sheet to create."]],"requires":["<code>openpyxl</code> installed"],"important":["recipient expects an Excel file"],"avoid":["using Excel as an intermediate format between two Python steps (Parquet is faster and lossless)"],"reach":"For deliverables intended to be opened in Excel.","example":"<pre>df.to_excel(<span class=\"s\">\"output/q1.xlsx\"</span>, sheet_name=<span class=\"s\">\"Q1\"</span>, index=<span class=\"k\">False</span>)</pre>","tags":["export","excel","xlsx","write","save"]},{"id":"to_parquet","code":"df.to_parquet(\"out.parquet\")","stage":"export","short":"Write the DataFrame to a Parquet file.","what":"Saves the DataFrame in a columnar binary format that preserves types and is substantially faster to read than CSV.","tokens":[[".to_parquet(...)","Method writing to Parquet."]],"requires":["<code>pyarrow</code> or <code>fastparquet</code> installed"],"important":["the data are to be re-read by another Python step","types must be preserved exactly"],"avoid":["choosing Parquet when the recipient cannot read it"],"reach":"For intermediate storage in a Python pipeline.","example":"<pre>df.to_parquet(<span class=\"s\">\"intermediates/cleaned_encounters.parquet\"</span>)</pre>","tags":["export","parquet","write","save","fast","binary"]},{"id":"corr","code":"df.corr(numeric_only=True)","stage":"stats","short":"Pairwise correlation matrix of numeric columns.","what":"Returns a square DataFrame of pairwise Pearson correlation coefficients between every pair of numeric columns, each in the range −1 to 1. Pearson measures linear association only; <code>method='spearman'</code> gives a rank correlation robust to monotone non-linearity.","tokens":[["df","The DataFrame whose numeric columns are correlated."],[".corr(...)","Computes the pairwise correlation matrix."],["numeric_only=True","Restricts the computation to numeric columns, avoiding errors on text."]],"requires":["a DataFrame with at least two numeric columns"],"important":["screening for linearly associated variables before modelling","a Spearman correlation is needed for monotone but non-linear relationships"],"avoid":["inferring causation from a correlation","interpreting Pearson on clearly non-linear relationships"],"reach":"When the strength of linear association between numeric variables must be screened, typically before regression or feature selection.","example":"<pre>c = df.corr(numeric_only=<span class=\"k\">True</span>)[<span class=\"s\">\"cost_eur\"</span>]</pre>","tags":["stats","corr","correlation","pearson","spearman","korrelation","association"]},{"id":"cov","code":"df.cov(numeric_only=True)","stage":"stats","short":"Pairwise covariance matrix of numeric columns.","what":"Returns a square DataFrame of covariances between numeric columns. Covariance shares the sign of the correlation but its magnitude depends on the variables' units and scales, so it is rarely interpreted directly — it is an input to other procedures.","tokens":[["df","The DataFrame whose numeric columns are used."],[".cov(...)","Computes the pairwise covariance matrix."],["numeric_only=True","Restricts to numeric columns."]],"requires":["a DataFrame with at least two numeric columns"],"important":["covariance is required as an input to a statistical method (PCA, portfolio variance)"],"avoid":["interpreting covariance magnitude directly — standardise to a correlation instead"],"reach":"When covariance is needed as an intermediate quantity for a downstream statistical computation.","example":"<pre>cov = df.cov(numeric_only=<span class=\"k\">True</span>)</pre>","tags":["stats","cov","covariance","kovarianz"]},{"id":"cumsum","code":"df[\"col\"].cumsum()","stage":"stats","short":"Running (cumulative) sum.","what":"Returns a Series of the same length in which each element is the sum of all values up to and including that position. Order-dependent: sort the data meaningfully (typically by date) before applying it.","tokens":[["df[\"col\"]","Selects the numeric column."],[".cumsum()","Produces the running total at each position."]],"requires":["a numeric Series","a meaningful row order (usually sorted by time)"],"important":["a cumulative total over time is required (running cost, cumulative admissions)"],"avoid":["the data is unordered or the row order is arbitrary — the result is then meaningless"],"reach":"When a running total along an ordered axis is required, such as cumulative cost over time.","example":"<pre>df[<span class=\"s\">\"cum_cost\"</span>] = df.sort_values(<span class=\"s\">\"date\"</span>)[<span class=\"s\">\"cost_eur\"</span>].cumsum()</pre>","tags":["stats","cumsum","cumulative","running total","kumulativ","laufende summe"]},{"id":"diff","code":"df[\"col\"].diff()","stage":"stats","short":"Difference from the previous row.","what":"Returns a Series of the difference between each element and the one before it; the first element is NaN because it has no predecessor. Order-dependent — sort by the time axis first. <code>periods=n</code> differences against the value n rows earlier.","tokens":[["df[\"col\"]","Selects the numeric column."],[".diff()","Subtracts each value's predecessor; first result is NaN."]],"requires":["a numeric Series","the rows ordered by the relevant axis (usually time)"],"important":["the period-on-period change (not the level) is the quantity of interest"],"avoid":["the data is unordered","forgetting that the first value is NaN in any downstream aggregation"],"reach":"When the change from one ordered observation to the next is required, such as daily change in admissions.","example":"<pre>df[<span class=\"s\">\"change\"</span>] = df.sort_values(<span class=\"s\">\"date\"</span>)[<span class=\"s\">\"admissions\"</span>].diff()</pre>","tags":["stats","diff","difference","change","differenz","veränderung"]},{"id":"idxmax","code":"df[\"col\"].idxmax()","stage":"stats","short":"Index label of the maximum value.","what":"Returns the index label (not the value, not the position) at which the column attains its maximum. On a DataFrame, <code>df.idxmax()</code> returns, per column, the index label of that column's maximum. Distinct from <code>max</code> (the value) and <code>argmax</code> (the integer position).","tokens":[["df[\"col\"]","Selects the column to search."],[".idxmax()","Returns the index label where the maximum occurs."]],"requires":["a Series with at least one non-missing value","a meaningful index (often set with <code>set_index</code>)"],"important":["the identity of the row achieving the maximum is wanted, not the maximum itself","the index has been set to a meaningful key"],"avoid":["the maximum value itself is needed (use <code>max</code>)","the positional location is needed (use <code>argmax</code>)","all values are NaN (raises)"],"reach":"When the question is which row attains the maximum — for example which ward has the longest mean stay.","example":"<pre>worst = df.groupby(<span class=\"s\">\"ward\"</span>)[<span class=\"s\">\"los_days\"</span>].mean().idxmax()</pre>","tags":["stats","idxmax","argmax","which","label","maximum","welcher"]},{"id":"idxmin","code":"df[\"col\"].idxmin()","stage":"stats","short":"Index label of the minimum value.","what":"Returns the index label at which the column attains its minimum — the counterpart of <code>idxmax</code>. Returns the label, not the value (<code>min</code>) and not the integer position (<code>argmin</code>).","tokens":[["df[\"col\"]","Selects the column to search."],[".idxmin()","Returns the index label where the minimum occurs."]],"requires":["a Series with at least one non-missing value","a meaningful index"],"important":["the identity of the row achieving the minimum is wanted"],"avoid":["the minimum value itself is needed (use <code>min</code>)","all values are NaN (raises)"],"reach":"When the question is which row attains the minimum, such as the least profitable DRG.","example":"<pre>cheapest = df.groupby(<span class=\"s\">\"drg_code\"</span>)[<span class=\"s\">\"cost_eur\"</span>].mean().idxmin()</pre>","tags":["stats","idxmin","argmin","which","label","minimum","welcher"]},{"id":"kurt","code":"df[\"col\"].kurt()","stage":"stats","short":"Tailedness of a distribution (excess kurtosis).","what":"Returns the excess kurtosis (Fisher definition): zero for a normal distribution, positive for heavier tails and a sharper peak, negative for lighter tails. Indicates the propensity for extreme values relative to the normal.","tokens":[["df[\"col\"]","Selects the numeric column."],[".kurt()","Returns the excess kurtosis (0 for a normal distribution)."]],"requires":["a numeric Series with at least four values"],"important":["assessing the prevalence of extreme values before relying on methods that assume normality"],"avoid":["interpreting kurtosis on a small sample, where it is highly variable"],"reach":"When the heaviness of a distribution's tails must be assessed, for instance before applying a method that assumes normality.","example":"<pre>k = df[<span class=\"s\">\"los_days\"</span>].kurt()</pre>","tags":["stats","kurt","kurtosis","kurtosis","tails","wölbung"]},{"id":"mean","code":"df[\"col\"].mean()","stage":"stats","short":"Arithmetic mean of a numeric Series.","what":"Returns the sum of the values divided by their count. NaN is skipped by default, so the mean is taken over the present values only. Sensitive to outliers: a single extreme value shifts it noticeably.","tokens":[["df[\"col\"]","Selects the numeric column to summarise."],[".mean()","Reduces the Series to a single number — the average."]],"requires":["a numeric (or boolean) Series"],"important":["the distribution is roughly symmetric, so the mean represents a typical value","the mean of a boolean Series is required — it is the proportion of <code>True</code>"],"avoid":["the distribution is skewed or has outliers (prefer <code>median</code>)","the column is categorical or an identifier"],"reach":"When a single representative value of a numeric column is required and the distribution is not strongly skewed.","example":"<pre>avg = df[<span class=\"s\">\"los_days\"</span>].mean()</pre>","tags":["stats","mean","average","durchschnitt","mittelwert","central"]},{"id":"median","code":"df[\"col\"].median()","stage":"stats","short":"Middle value of a numeric Series.","what":"Returns the value separating the higher half from the lower half of the sorted column. Robust to outliers and skew: unlike the mean, extreme values do not pull it.","tokens":[["df[\"col\"]","Selects the numeric column."],[".median()","Reduces to the central value of the sorted data."]],"requires":["a numeric Series"],"important":["the distribution is skewed or contains outliers","a robust measure of central tendency is required for reporting"],"avoid":["a value that can be arithmetically combined downstream is needed and the data is symmetric (the mean is then conventional)"],"reach":"When a typical value is required and the column is skewed or contains outliers — the default choice for length-of-stay, cost, or income style data.","example":"<pre>typical = df[<span class=\"s\">\"cost_eur\"</span>].median()</pre>","tags":["stats","median","zentralwert","robust","central","middle"]},{"id":"mode","code":"df[\"col\"].mode()","stage":"stats","short":"Most frequent value(s) of a Series.","what":"Returns a Series of the value(s) that occur most often. The result may contain more than one entry when several values tie for the highest frequency, which is why a Series rather than a scalar is returned.","tokens":[["df[\"col\"]","Selects the column, numeric or categorical."],[".mode()","Returns the most frequent value(s) as a Series."]],"requires":["a Series with at least one non-missing value"],"important":["the column is categorical and the most common category is of interest","a single value is required — take <code>.mode().iloc[0]</code>, acknowledging ties"],"avoid":["a continuous numeric column where no value repeats meaningfully"],"reach":"When the most common category or value is the quantity of interest, typically for categorical data.","example":"<pre>most_common = df[<span class=\"s\">\"ward\"</span>].mode().iloc[<span class=\"s\">0</span>]</pre>","tags":["stats","mode","frequent","modus","häufigster","categorical"]},{"id":"nunique","code":"df[\"col\"].nunique()","stage":"stats","short":"Number of distinct values.","what":"Returns the count of distinct non-missing values in the column. NaN is excluded by default; pass <code>dropna=False</code> to count it as one further value. Used to gauge cardinality before grouping, pivoting, or treating a column as categorical.","tokens":[["df[\"col\"]","Selects the column."],[".nunique()","Counts how many distinct values it contains."]],"requires":["a Series"],"important":["the cardinality of a key must be checked before a pivot or a wide reshape","deciding whether a column is effectively categorical"],"avoid":["confusing it with <code>count</code> (number of non-missing rows) — <code>nunique</code> counts distinct values"],"reach":"When the number of distinct categories or keys must be known, typically before grouping or reshaping.","example":"<pre>n_wards = df[<span class=\"s\">\"ward\"</span>].nunique()</pre>","tags":["stats","nunique","distinct","unique","cardinality","verschiedene"]},{"id":"pct_change","code":"df[\"col\"].pct_change()","stage":"stats","short":"Fractional change from the previous row.","what":"Returns the fractional change between each element and the previous one: (xₜ − xₜ₋₁) / xₜ₋₁. The first element is NaN. Multiply by 100 for a percentage. Order-dependent and undefined where the previous value is zero.","tokens":[["df[\"col\"]","Selects the numeric column."],[".pct_change()","Computes the fractional change against the previous row."]],"requires":["a numeric Series ordered by the relevant axis","non-zero previous values"],"important":["a growth rate rather than an absolute change is required"],"avoid":["the previous value can be zero (division yields inf or NaN)","the data is unordered"],"reach":"When a period-on-period growth rate is required, such as month-on-month change in cost.","example":"<pre>df[<span class=\"s\">\"growth\"</span>] = df.sort_values(<span class=\"s\">\"month\"</span>)[<span class=\"s\">\"cost_eur\"</span>].pct_change()</pre>","tags":["stats","pct_change","growth","percentage change","wachstum","rate"]},{"id":"quantile","code":"df[\"col\"].quantile([0.25, 0.5, 0.75])","stage":"stats","short":"Value(s) at the given quantile position(s).","what":"Returns the value below which the given fraction of the data falls. A scalar argument returns one value; a list returns a Series indexed by the requested quantiles. The interquartile range is <code>quantile(0.75) − quantile(0.25)</code>.","tokens":[["df[\"col\"]","Selects the numeric column."],[".quantile(...)","Computes the value at the requested quantile(s)."],["[0.25, 0.5, 0.75]","The quantile positions; here the three quartiles."]],"requires":["a numeric Series","quantile arguments in the range 0 to 1"],"important":["a robust description of spread is required (the IQR)","thresholds such as the top decile must be located"],"avoid":["a very small sample, where quantile estimates are unstable"],"reach":"When robust spread (the IQR) or a distributional threshold such as a percentile cut-off is required.","example":"<pre>q1, q3 = df[<span class=\"s\">\"cost_eur\"</span>].quantile([<span class=\"s\">0.25</span>, <span class=\"s\">0.75</span>])\niqr = q3 - q1</pre>","tags":["stats","quantile","percentile","quartile","iqr","quantil","threshold"]},{"id":"rank","code":"df[\"col\"].rank(method=\"dense\", ascending=False)","stage":"stats","short":"Ordinal rank of each value.","what":"Returns a Series assigning each value its rank within the column. <code>method</code> controls tie handling: <code>'average'</code> (default) averages tied ranks, <code>'min'</code> assigns the lowest, <code>'dense'</code> leaves no gaps after ties. <code>ascending=False</code> ranks the largest value first.","tokens":[["df[\"col\"]","Selects the numeric column to rank."],[".rank(...)","Assigns an ordinal rank to each value."],["method=\"dense\"","Tie policy: equal values share a rank and the next rank is not skipped."],["ascending=False","Largest value receives rank 1."]],"requires":["a numeric Series","an explicit tie policy via <code>method</code> when ties are likely"],"important":["a ranking (league table) is required rather than the raw values","ties must be handled by a stated rule"],"avoid":["leaving the tie policy implicit when ties matter to the result"],"reach":"When values must be turned into a ranking, for instance to build a league table of wards by cost.","example":"<pre>df[<span class=\"s\">\"rank\"</span>] = df[<span class=\"s\">\"cost_eur\"</span>].rank(method=<span class=\"s\">\"dense\"</span>, ascending=<span class=\"k\">False</span>)</pre>","tags":["stats","rank","ranking","rang","league table","ordinal","ties"]},{"id":"skew","code":"df[\"col\"].skew()","stage":"stats","short":"Asymmetry of a distribution.","what":"Returns the skewness coefficient. Approximately zero indicates a symmetric distribution; positive indicates a long right tail (mean above median); negative a long left tail. A useful numeric check of whether the mean or the median is the better summary.","tokens":[["df[\"col\"]","Selects the numeric column."],[".skew()","Returns the skewness coefficient."]],"requires":["a numeric Series with at least three values"],"important":["deciding between mean and median as the central summary","checking a modelling assumption of symmetry"],"avoid":["interpreting skew on a very small sample"],"reach":"When the symmetry of a distribution must be quantified, often to justify choosing the median over the mean.","example":"<pre>s = df[<span class=\"s\">\"cost_eur\"</span>].skew()</pre>","tags":["stats","skew","skewness","schiefe","asymmetry","distribution"]},{"id":"std","code":"df[\"col\"].std()","stage":"stats","short":"Sample standard deviation of a Series.","what":"Returns the spread of the values about their mean, in the same unit as the data. pandas uses the sample standard deviation (divisor n−1) by default; pass <code>ddof=0</code> for the population value.","tokens":[["df[\"col\"]","Selects the numeric column."],[".std()","Reduces to the sample standard deviation (<code>ddof=1</code>)."]],"requires":["a numeric Series with at least two values"],"important":["the dispersion of a roughly symmetric distribution is required","results are compared with the mean (same unit)"],"avoid":["the distribution is heavily skewed (the IQR via <code>quantile</code> is more interpretable)","comparing spread across columns on different scales without standardising"],"reach":"When the variability of a numeric column must be quantified alongside its mean.","example":"<pre>spread = df[<span class=\"s\">\"cost_eur\"</span>].std()</pre>","tags":["stats","std","standard deviation","spread","streuung","dispersion","ddof"]},{"id":"unique","code":"df[\"col\"].unique()","stage":"stats","short":"The distinct values themselves, as an array.","what":"Returns a NumPy array of the distinct values in order of first appearance (not sorted). Use it to inspect which categories actually occur — for example to discover inconsistent spellings before normalising.","tokens":[["df[\"col\"]","Selects the column."],[".unique()","Returns the distinct values as an array, in order of appearance."]],"requires":["a Series"],"important":["the actual set of categories must be inspected (e.g. to find inconsistent labels)"],"avoid":["only the number of distinct values is needed (use <code>nunique</code>)","a sorted, counted view is wanted (use <code>value_counts</code>)"],"reach":"When the concrete set of values present in a column must be examined, often during cleaning.","example":"<pre>labels = df[<span class=\"s\">\"gender\"</span>].unique()</pre>","tags":["stats","unique","distinct","categories","verschiedene","inspect"]},{"id":"value_counts_normalize","code":"df[\"col\"].value_counts(normalize=True)","stage":"stats","short":"Relative frequency of each category.","what":"Returns a Series whose index is the distinct values and whose values are their proportions of the total, sorted descending. Equivalent to <code>value_counts()</code> divided by the count, but computed in one step. NaN is excluded unless <code>dropna=False</code>.","tokens":[["df[\"col\"]","Selects the column whose category proportions are wanted."],[".value_counts(...)","Counts occurrences of each distinct value."],["normalize=True","Returns proportions (summing to 1) rather than raw counts."]],"requires":["a Series, typically categorical or low-cardinality"],"important":["the share of each category is more informative than the raw count","class balance must be assessed before modelling"],"avoid":["a continuous numeric column with many distinct values (the result is long and uninformative)"],"reach":"When the proportion of each category — rather than its absolute count — answers the question, such as assessing class balance.","example":"<pre>shares = df[<span class=\"s\">\"ward\"</span>].value_counts(normalize=<span class=\"k\">True</span>)</pre>","tags":["stats","value_counts","normalize","proportion","frequency","anteil","balance"]},{"id":"var","code":"df[\"col\"].var()","stage":"stats","short":"Sample variance of a Series.","what":"Returns the mean squared deviation from the mean, the square of the standard deviation. Its unit is the square of the data's unit, which makes the standard deviation usually preferable for reporting.","tokens":[["df[\"col\"]","Selects the numeric column."],[".var()","Reduces to the sample variance (<code>ddof=1</code>)."]],"requires":["a numeric Series with at least two values"],"important":["variance is required directly, for example in a statistical formula or an ANOVA-style decomposition"],"avoid":["reporting spread to a reader — the standard deviation is in the data's own unit and is more interpretable"],"reach":"When variance is needed as an input to a further statistical computation rather than for direct interpretation.","example":"<pre>v = df[<span class=\"s\">\"los_days\"</span>].var()</pre>","tags":["stats","var","variance","varianz","spread","ddof"]},{"id":"at","code":"df.at[label, \"col\"]","stage":"select","short":"Fast access to a single cell by label.","what":"Reads or writes one scalar cell, addressed by row label and column name. Faster than <code>loc</code> for a single value because it does no alignment or slicing — but it works only for exactly one cell.","tokens":[["df.at[","Scalar label accessor (single cell only)."],["label","The row's index label."],["\"col\"","The column name."]],"requires":["a unique row label and a column name","the target being a single cell, not a range"],"important":["one specific cell must be read or updated in a tight loop or hot path"],"avoid":["selecting more than one cell (use <code>loc</code>)","the label is not unique"],"reach":"When exactly one cell must be read or set and performance of repeated single-cell access matters.","example":"<pre>df.at[<span class=\"s\">42</span>, <span class=\"s\">\"status\"</span>] = <span class=\"s\">\"closed\"</span></pre>","tags":["select","at","scalar","single cell","fast","zelle"]},{"id":"boolean_indexing","code":"df[df[\"age\"] > 65]","stage":"select","short":"Select rows with a boolean mask.","what":"A comparison on a column produces a boolean Series (the mask); indexing the DataFrame with it returns the rows where the mask is True. Combine conditions with <code>&</code> (and), <code>|</code> (or), <code>~</code> (not), each operand wrapped in parentheses because these operators bind tighter than comparison.","tokens":[["df[\"age\"] > 65","A comparison yielding a boolean Series — True where the row qualifies."],["df[ ... ]","Selecting with that boolean Series keeps only the True rows."]],"requires":["a condition that evaluates to a boolean Series aligned to the rows"],"important":["rows must be restricted to a subpopulation defined by their values","combining conditions — parenthesise each: <code>(a) & (b)</code>"],"avoid":["using Python <code>and</code> / <code>or</code> instead of <code>&</code> / <code>|</code> (raises on Series)","assigning into the result of <code>df[mask]</code> (chained assignment — use <code>df.loc[mask, col] = …</code>)"],"reach":"Whenever the analysis concerns a subset of rows defined by a condition on the data — the most common filtering operation.","example":"<pre>icu_elderly = df[(df[<span class=\"s\">\"age\"</span>] &gt; <span class=\"s\">65</span>) &amp; (df[<span class=\"s\">\"ward\"</span>] == <span class=\"s\">\"ICU\"</span>)]</pre>","tags":["select","boolean","mask","filter","condition","maske","bedingung"]},{"id":"copy_vs_view","code":"subset = df[df.a > 0].copy()","stage":"select","short":"Take an explicit copy to avoid SettingWithCopyWarning.","what":"An indexing expression may return either a view onto the original DataFrame or a fresh copy, and pandas does not promise which. Assigning into an object whose origin is ambiguous triggers <code>SettingWithCopyWarning</code> and may silently fail to write back. The remedy is to be explicit: take <code>.copy()</code> when an independent subset is intended, or assign through <code>df.loc[rows, col] = value</code> when the original must be modified.","tokens":[["df[df.a > 0]","A filtered subset whose copy/view status is not guaranteed."],[".copy()","Forces an independent copy, so later assignment is well-defined and warning-free."]],"requires":["awareness of whether the original or an independent subset is intended"],"important":["a filtered subset will be modified independently of the original — take <code>.copy()</code>","the original must be updated — assign via <code>df.loc[mask, col] = …</code> instead"],"avoid":["chained assignment such as <code>df[df.a>0]['b'] = 1</code> — it writes into a temporary and is the classic cause of <code>SettingWithCopyWarning</code>"],"reach":"Whenever a filtered or sliced subset is taken and then assigned into — the single most common source of confusing pandas warnings.","example":"<pre>elderly = df[df[<span class=\"s\">\"age\"</span>] &gt; <span class=\"s\">65</span>].copy()\nelderly[<span class=\"s\">\"flag\"</span>] = <span class=\"k\">True</span>   <span class=\"c\"># safe: independent copy</span></pre>","tags":["select","copy","view","SettingWithCopyWarning","chained assignment","kopie","ansicht"]},{"id":"iat","code":"df.iat[row_pos, col_pos]","stage":"select","short":"Fast access to a single cell by position.","what":"The positional counterpart of <code>at</code>: reads or writes one scalar cell addressed by integer row and column positions. Fastest single-cell accessor when the position, not the label, is known.","tokens":[["df.iat[","Scalar position accessor (single cell only)."],["row_pos","Integer row position."],["col_pos","Integer column position."]],"requires":["the integer position of exactly one cell"],"important":["a single cell at a known position must be read or written efficiently"],"avoid":["selecting ranges (use <code>iloc</code>)","the position is not known but the label is (use <code>at</code>)"],"reach":"When a single cell must be accessed by position, typically inside performance-sensitive code.","example":"<pre>v = df.iat[<span class=\"s\">0</span>, <span class=\"s\">3</span>]</pre>","tags":["select","iat","scalar","single cell","position","fast"]},{"id":"iloc","code":"df.iloc[row_pos, col_pos]","stage":"select","short":"Select rows and columns by integer position.","what":"Position-based selection, independent of the index labels. Uses ordinary zero-based Python slicing, so the end of a slice is exclusive. Use it when the position matters (the first row, the last five) rather than the label.","tokens":[["df.iloc[","Position-based indexer."],["row_pos","Integer position(s) or a half-open slice selecting rows."],["col_pos","Integer column position(s); omit to keep all columns."]],"requires":["the positional location of the rows or columns of interest"],"important":["the first/last n rows are wanted regardless of the index","the index labels are not meaningful or not unique"],"avoid":["selecting by a meaningful key (use <code>loc</code>)","expecting an inclusive slice end — <code>iloc</code> is exclusive like Python"],"reach":"When rows or columns must be taken by position, such as the first row of each group or the last five records.","example":"<pre>first_row = df.iloc[<span class=\"s\">0</span>]\nlast_five = df.iloc[<span class=\"s\">-5</span>:]</pre>","tags":["select","iloc","position","integer","slicing","auswahl","position"]},{"id":"loc","code":"df.loc[row_labels, col_labels]","stage":"select","short":"Select rows and columns by label.","what":"Label-based selection. The first position selects rows by index label (or a boolean mask), the second selects columns by name. Both endpoints of a label slice are inclusive — unlike Python slicing. <code>loc</code> is also the correct left-hand side for writing values back into a DataFrame.","tokens":[["df.loc[","Label-based indexer."],["row_labels","Index labels, a label slice (inclusive), or a boolean mask selecting rows."],["col_labels","Column name(s) to keep; omit to keep all columns."]],"requires":["knowledge of the index labels and column names","for a label slice, a sorted or otherwise meaningful index"],"important":["selecting by a meaningful index (date, identifier) rather than position","writing values into a subset — <code>df.loc[mask, 'col'] = value</code> is the safe assignment form"],"avoid":["using integer positions here (that is <code>iloc</code>) unless the index itself is integer labels","expecting an exclusive end on a label slice — it is inclusive"],"reach":"When rows or cells must be selected by their labels, or when assigning into a filtered subset without the chained-assignment trap.","example":"<pre>df.loc[df[<span class=\"s\">\"age\"</span>] &gt; <span class=\"s\">65</span>, <span class=\"s\">\"risk\"</span>] = <span class=\"s\">\"high\"</span></pre>","tags":["select","loc","label","indexing","assignment","auswahl","etikett"]},{"id":"reset_index","code":"df.reset_index(drop=True)","stage":"select","short":"Restore a default 0…n−1 integer index.","what":"Returns a DataFrame whose index is renumbered to a clean RangeIndex. After filtering or dropping rows the index has gaps; <code>reset_index</code> renumbers them. <code>drop=True</code> discards the old index; the default <code>drop=False</code> keeps it as a new column — important after <code>groupby</code>, where the keys live in the index.","tokens":[["df.reset_index(","Replaces the index with a fresh 0…n−1 RangeIndex."],["drop=True","Discards the old index instead of keeping it as a column."]],"requires":["a DataFrame whose index has gaps or carries grouping keys"],"important":["after <code>dropna</code> / boolean filtering, to restore consecutive row numbers","after <code>groupby</code>, to turn the grouped keys back into ordinary columns (use the default <code>drop=False</code>)"],"avoid":["<code>drop=True</code> after a groupby — it would discard the grouping keys you need"],"reach":"After filtering, dropping rows, or grouping, when a clean integer index or the grouped keys as columns are required.","example":"<pre>clean = df.dropna(subset=[<span class=\"s\">\"los_days\"</span>]).reset_index(drop=<span class=\"k\">True</span>)</pre>","tags":["select","reset_index","index","renumber","groupby","zurücksetzen"]},{"id":"set_index","code":"df.set_index(\"patient_id\")","stage":"select","short":"Promote a column to be the row index.","what":"Returns a DataFrame whose row index becomes the values of the named column, which is removed from the body. A meaningful index enables label-based selection with <code>loc</code> and aligns operations on that key. <code>append=True</code> adds it as a further index level instead of replacing.","tokens":[["df.set_index(","Sets the row index from existing column(s)."],["\"patient_id\"","The column to become the index."]],"requires":["a column suitable as a key (often unique, e.g. an identifier or a date)"],"important":["subsequent selection or alignment is naturally by this key","preparing for a join or a time-series operation keyed on the index"],"avoid":["setting a non-unique index when uniqueness is later assumed","losing the column unintentionally — it leaves the body unless <code>drop=False</code>"],"reach":"When a column is the natural key for selection, alignment, or joining and should become the index.","example":"<pre>indexed = df.set_index(<span class=\"s\">\"patient_id\"</span>)</pre>","tags":["select","set_index","index","key","schlüssel","promote"]},{"id":"str_cat","code":"df[\"a\"].str.cat(df[\"b\"], sep=\"-\")","stage":"strings","short":"Concatenate strings element-wise.","what":"Joins the calling Series with another (or a list of others) element by element, optionally separated by <code>sep</code>. With <code>na_rep</code> a placeholder replaces missing values; otherwise a NaN on either side propagates to the result.","tokens":[["df[\"a\"]","The first text column."],[".str.cat(","Concatenates element-wise with the argument."],["df[\"b\"], sep=\"-\"","The second column and the separator placed between values."]],"requires":["aligned Series of strings","an <code>na_rep</code> if either side may be missing"],"important":["a composite key or label must be built from several columns"],"avoid":["NaN on either side without <code>na_rep</code> (the row becomes NaN silently)"],"reach":"When a single string must be assembled from several columns, for instance a composite key.","example":"<pre>df[<span class=\"s\">\"key\"</span>] = df[<span class=\"s\">\"ward\"</span>].str.cat(df[<span class=\"s\">\"drg_code\"</span>], sep=<span class=\"s\">\"-\"</span>)</pre>","tags":["strings","str","cat","concatenate","join","verketten"]},{"id":"str_contains","code":"df[\"col\"].str.contains(\"icu\", case=False, na=False)","stage":"strings","short":"Boolean mask: which strings contain a pattern.","what":"Returns a boolean Series, True where the string contains the pattern. The pattern is a regular expression by default; pass <code>regex=False</code> for a literal substring. <code>na=False</code> treats missing values as non-matches so the mask is safe for row selection.","tokens":[["df[\"col\"]","Selects the text column."],[".str","The string accessor — applies string methods element-wise."],[".contains(\"icu\", ...)","Tests each value for the pattern."],["case=False","Case-insensitive match."],["na=False","Missing values become False rather than NaN, so the mask is usable for indexing."]],"requires":["a Series of strings (dtype object or string)","<code>na=False</code> if the result will index rows"],"important":["rows must be filtered by a substring or pattern in a text column","matching must ignore case"],"avoid":["leaving <code>na=False</code> off when the mask selects rows (NaN would raise)","a regex metacharacter is meant literally (set <code>regex=False</code>)"],"reach":"When rows must be selected by the presence of a substring or pattern within a text column.","example":"<pre>icu = df[df[<span class=\"s\">\"ward\"</span>].str.contains(<span class=\"s\">\"icu\"</span>, case=<span class=\"k\">False</span>, na=<span class=\"k\">False</span>)]</pre>","tags":["strings","str","contains","substring","regex","filter","enthält"]},{"id":"str_endswith","code":"df[\"col\"].str.endswith(\".csv\")","stage":"strings","short":"Boolean mask: which strings end with a suffix.","what":"Returns a boolean Series, True where the string ends with the given literal suffix. The counterpart of <code>startswith</code>; commonly used on filenames or coded identifiers.","tokens":[["df[\"col\"]","Selects the text column."],[".str.endswith(","Tests each value for the literal suffix."],["\".csv\"","The suffix to test for."]],"requires":["a Series of strings"],"important":["rows are identified by a trailing token, such as a file extension"],"avoid":["pattern matching beyond a fixed suffix is required (use <code>str.contains</code>)"],"reach":"When membership is defined by a fixed trailing token such as a file extension.","example":"<pre>csvs = df[df[<span class=\"s\">\"filename\"</span>].str.endswith(<span class=\"s\">\".csv\"</span>)]</pre>","tags":["strings","str","endswith","suffix","extension","endung"]},{"id":"str_extract","code":"df[\"col\"].str.extract(r\"(\\d+)\")","stage":"strings","short":"Pull a regex capture group out of each string.","what":"Applies a regular expression containing capture groups and returns the captured text — a Series for one group, a DataFrame for several. The canonical way to lift a structured fragment (a number, a code) out of free text.","tokens":[["df[\"col\"]","Selects the text column."],[".str.extract(","Applies a regex and returns its capture group(s)."],["r\"(\\d+)\"","A raw-string regex; the parentheses capture one run of digits."]],"requires":["a Series of strings","a regular expression with at least one capture group"],"important":["a structured token (year, numeric id) must be lifted out of free text"],"avoid":["a regex with no capture group (returns all NaN)","expecting multiple matches per string (use <code>str.findall</code>)"],"reach":"When a specific structured fragment must be parsed out of an otherwise free-text column.","example":"<pre>df[<span class=\"s\">\"year\"</span>] = df[<span class=\"s\">\"label\"</span>].str.extract(r<span class=\"s\">\"(\\d{4})\"</span>)</pre>","tags":["strings","str","extract","regex","capture","parse","extrahieren"]},{"id":"str_findall","code":"df[\"col\"].str.findall(r\"\\d+\")","stage":"strings","short":"All regex matches per string, as a list.","what":"Returns a Series whose every element is the list of all non-overlapping matches of the pattern in that string. Use it when a string may contain several occurrences; follow with <code>explode</code> to obtain one row per match.","tokens":[["df[\"col\"]","Selects the text column."],[".str.findall(","Finds every match of the pattern in each value."],["r\"\\d+\"","A raw-string regex matching runs of digits."]],"requires":["a Series of strings","a regular expression"],"important":["each string may contain several matches and all are needed"],"avoid":["only one match is expected (use <code>str.extract</code>)"],"reach":"When every occurrence of a pattern within each string is required, not just the first.","example":"<pre>nums = df[<span class=\"s\">\"note\"</span>].str.findall(r<span class=\"s\">\"\\d+\"</span>)</pre>","tags":["strings","str","findall","regex","all matches","list","alle"]},{"id":"str_len","code":"df[\"col\"].str.len()","stage":"strings","short":"Character length of each string.","what":"Returns an integer Series giving the number of characters in each string. NaN remains NaN. Used to validate fixed-width codes or to flag suspiciously short or long entries.","tokens":[["df[\"col\"]","Selects the text column."],[".str.len()","Returns the length of each string."]],"requires":["a Series of strings"],"important":["fixed-width codes must be validated, or unusually short/long values flagged"],"avoid":["confusing it with <code>Series.size</code> / <code>len(df)</code>, which count rows, not characters"],"reach":"When the length of text values carries information, such as validating identifier formats.","example":"<pre>bad = df[df[<span class=\"s\">\"code\"</span>].str.len() != <span class=\"s\">3</span>]</pre>","tags":["strings","str","len","length","validate","länge"]},{"id":"str_split","code":"df[\"col\"].str.split(\"-\", expand=True)","stage":"strings","short":"Split each string on a delimiter.","what":"Splits every string at the delimiter. Without arguments the result is a Series of lists; with <code>expand=True</code> it becomes a DataFrame, one column per field — the usual way to break a compound code into its parts. <code>n=1</code> limits the number of splits.","tokens":[["df[\"col\"]","Selects the text column."],[".str.split(\"-\", ...)","Splits each value at every '-'."],["expand=True","Returns a DataFrame of columns instead of a Series of lists."]],"requires":["a Series of strings with a consistent delimiter"],"important":["a compound field must be decomposed into separate columns","only the first split is wanted (use <code>n=1</code>)"],"avoid":["the delimiter is inconsistent across rows (the resulting columns misalign)"],"reach":"When a single column encodes several fields joined by a delimiter that must be separated.","example":"<pre>parts = df[<span class=\"s\">\"code\"</span>].str.split(<span class=\"s\">\"-\"</span>, expand=<span class=\"k\">True</span>)</pre>","tags":["strings","str","split","delimiter","expand","trennen"]},{"id":"str_startswith","code":"df[\"col\"].str.startswith(\"A\")","stage":"strings","short":"Boolean mask: which strings begin with a prefix.","what":"Returns a boolean Series, True where the string starts with the given literal prefix (not a regex). NaN yields NaN unless an <code>na</code> value is supplied.","tokens":[["df[\"col\"]","Selects the text column."],[".str.startswith(","Tests each value for the literal prefix."],["\"A\"","The prefix to test for."]],"requires":["a Series of strings","an <code>na=False</code> argument if the result indexes rows"],"important":["rows are identified by a fixed leading code or prefix (e.g. DRG codes beginning 'A')"],"avoid":["a regular expression is needed (use <code>str.contains</code> with an anchored pattern)"],"reach":"When membership is defined by a fixed prefix, such as identifier codes sharing a leading letter.","example":"<pre>a_codes = df[df[<span class=\"s\">\"drg_code\"</span>].str.startswith(<span class=\"s\">\"A\"</span>)]</pre>","tags":["strings","str","startswith","prefix","präfix","code"]},{"id":"date_range","code":"pd.date_range(\"2026-01-01\", periods=12, freq=\"MS\")","stage":"datetime","short":"Generate a regular sequence of dates.","what":"Creates a DatetimeIndex of evenly spaced dates. Specify a start with either <code>periods=</code> (a count) or <code>end=</code>, and a frequency (<code>'D'</code> day, <code>'MS'</code> month-start, <code>'W'</code> week). Used to build a complete calendar against which sparse data can be reindexed so missing periods become visible.","tokens":[["pd.date_range(","Builds a regular sequence of dates."],["\"2026-01-01\"","The start date."],["periods=12","How many dates to generate."],["freq=\"MS\"","The spacing — here the first day of each month."]],"requires":["a start, and either a period count or an end","a frequency string"],"important":["a complete date axis is needed so that gaps in the data become explicit (reindex onto it)"],"avoid":["confusing <code>'M'</code> (month-end) with <code>'MS'</code> (month-start)"],"reach":"When a complete, regular calendar is required, typically to reveal missing periods in a time series.","example":"<pre>cal = pd.date_range(<span class=\"s\">\"2026-01-01\"</span>, periods=<span class=\"s\">12</span>, freq=<span class=\"s\">\"MS\"</span>)</pre>","tags":["datetime","date_range","calendar","sequence","freq","kalender"]},{"id":"dt_hour","code":"df[\"ts\"].dt.hour","stage":"datetime","short":"Extract the hour of day (0–23).","what":"Returns an integer Series of the hour component, 0 to 23. Used to study intraday patterns such as admissions by time of day.","tokens":[["df[\"ts\"]","A datetime64 column including a time component."],[".dt.hour","The hour of day, 0–23."]],"requires":["a datetime64 column that carries a time, not only a date"],"important":["intraday (hour-of-day) patterns are of interest"],"avoid":["a date-only column, where the hour is always 0"],"reach":"When the time of day matters, for instance staffing by hour.","example":"<pre>by_hour = df.groupby(df[<span class=\"s\">\"ts\"</span>].dt.hour).size()</pre>","tags":["datetime","dt","hour","stunde","intraday"]},{"id":"dt_month","code":"df[\"date\"].dt.month","stage":"datetime","short":"Extract the month number (1–12).","what":"Returns an integer Series of month numbers, 1 for January through 12 for December. For a human-readable name use <code>.dt.month_name()</code>; for a sortable year-month period use <code>.dt.to_period('M')</code>.","tokens":[["df[\"date\"]","A datetime64 column."],[".dt.month","The month number, 1–12."]],"requires":["a datetime64 column"],"important":["seasonality by month is of interest"],"avoid":["using the bare month number across multiple years (it conflates Januarys — use <code>to_period</code>)"],"reach":"When monthly seasonality within a year is the question.","example":"<pre>by_month = df.groupby(df[<span class=\"s\">\"admitted\"</span>].dt.month).size()</pre>","tags":["datetime","dt","month","monat","season"]},{"id":"dt_strftime","code":"df[\"date\"].dt.strftime(\"%Y-%m\")","stage":"datetime","short":"Format datetimes as strings.","what":"Formats each timestamp as a string according to the given pattern (<code>%Y</code> year, <code>%m</code> month, <code>%d</code> day, …). The result is text — convenient for labels and display, but no longer usable for date arithmetic or chronological sorting.","tokens":[["df[\"date\"]","A datetime64 column."],[".dt.strftime(\"%Y-%m\")","Formats each timestamp as a 'YYYY-MM' string."]],"requires":["a datetime64 column","a valid strftime format string"],"important":["a human-readable label is needed for a chart axis or a report"],"avoid":["formatting before doing date arithmetic or sorting — the result is text and no longer chronological","for a sortable period prefer <code>to_period</code>"],"reach":"When a date must be rendered as a label for display, after any date computation is finished.","example":"<pre>df[<span class=\"s\">\"label\"</span>] = df[<span class=\"s\">\"admitted\"</span>].dt.strftime(<span class=\"s\">\"%Y-%m\"</span>)</pre>","tags":["datetime","dt","strftime","format","label","formatieren"]},{"id":"dt_to_period","code":"df[\"date\"].dt.to_period(\"M\")","stage":"datetime","short":"Collapse timestamps to a calendar period.","what":"Converts timestamps to Period objects at the chosen frequency ('M' month, 'Q' quarter, 'Y' year). Unlike a formatted string, a Period sorts chronologically and groups correctly across years — the right key for monthly or quarterly aggregation.","tokens":[["df[\"date\"]","A datetime64 column."],[".dt.to_period(\"M\")","Maps each timestamp to its calendar month, as a sortable Period."]],"requires":["a datetime64 column"],"important":["monthly or quarterly aggregation must remain correctly ordered across years"],"avoid":["using <code>strftime</code> for a grouping key (string months sort lexically, not chronologically across years)"],"reach":"When aggregating by month or quarter and the period must sort and group correctly across years.","example":"<pre>monthly = df.groupby(df[<span class=\"s\">\"admitted\"</span>].dt.to_period(<span class=\"s\">\"M\"</span>)).size()</pre>","tags":["datetime","dt","to_period","period","month","quarter","periode"]},{"id":"dt_weekday","code":"df[\"date\"].dt.weekday","stage":"datetime","short":"Day of week as an integer (Monday=0).","what":"Returns an integer Series with Monday as 0 through Sunday as 6. <code>.dt.day_name()</code> gives the textual name. Typically used to contrast weekday and weekend behaviour.","tokens":[["df[\"date\"]","A datetime64 column."],[".dt.weekday","Day of week, Monday=0 … Sunday=6."]],"requires":["a datetime64 column"],"important":["weekday versus weekend patterns must be compared"],"avoid":["assuming Sunday=0 (pandas uses Monday=0)"],"reach":"When behaviour differs by day of week, such as weekday versus weekend admissions.","example":"<pre>df[<span class=\"s\">\"is_weekend\"</span>] = df[<span class=\"s\">\"admitted\"</span>].dt.weekday &gt;= <span class=\"s\">5</span></pre>","tags":["datetime","dt","weekday","dayofweek","wochentag","weekend"]},{"id":"dt_year","code":"df[\"date\"].dt.year","stage":"datetime","short":"Extract the year from a datetime column.","what":"The <code>.dt</code> accessor exposes datetime components element-wise. <code>.dt.year</code> returns an integer Series of calendar years — the basis for grouping or filtering by year.","tokens":[["df[\"date\"]","A datetime64 column."],[".dt","The datetime accessor."],[".year","The calendar year of each timestamp."]],"requires":["a column already of dtype datetime64 (parse with <code>pd.to_datetime</code> first)"],"important":["aggregating or filtering by year is required"],"avoid":["calling <code>.dt</code> on an object/string column (parse it first)"],"reach":"When the analysis must be broken down or filtered by calendar year.","example":"<pre>per_year = df.groupby(df[<span class=\"s\">\"admitted\"</span>].dt.year).size()</pre>","tags":["datetime","dt","year","jahr","extract"]},{"id":"resample","code":"df.set_index(\"date\").resample(\"M\")[\"cost\"].sum()","stage":"datetime","short":"Group a time series into regular time buckets.","what":"The time-series analogue of <code>groupby</code>: with a datetime index, <code>resample</code> bins rows into regular intervals (<code>'D'</code>, <code>'W'</code>, <code>'M'</code>, …) and an aggregation summarises each bin. Downsampling reduces frequency (days → months); it produces a row for every interval, including empty ones.","tokens":[["df.set_index(\"date\")","A datetime index is required for resampling."],[".resample(\"M\")","Bins the rows into monthly intervals."],["[\"cost\"].sum()","Aggregates the chosen column within each interval."]],"requires":["a DatetimeIndex (set it with <code>set_index</code> on a datetime column)","an aggregation function"],"important":["a time series must be summarised at a coarser, regular frequency","empty intervals must appear explicitly (resample emits them)"],"avoid":["resampling without a datetime index (raises)","confusing it with plain <code>groupby</code> on <code>dt.month</code>, which omits empty months"],"reach":"When a time series must be aggregated into regular calendar buckets, such as monthly totals from daily records.","example":"<pre>monthly = (df.set_index(<span class=\"s\">\"admitted\"</span>)\n             .resample(<span class=\"s\">\"M\"</span>)[<span class=\"s\">\"cost_eur\"</span>].sum())</pre>","tags":["datetime","resample","time series","downsample","frequency","zeitreihe"]},{"id":"crosstab","code":"pd.crosstab(df[\"ward\"], df[\"gender\"])","stage":"reshape","short":"Frequency table of two categorical variables.","what":"Builds a contingency table: rows are the distinct values of the first argument, columns those of the second, cells the counts of co-occurrence. <code>normalize='index'</code> turns counts into row proportions; <code>values=</code> with <code>aggfunc=</code> summarises a third column instead of counting.","tokens":[["pd.crosstab(","Builds a cross-tabulation of two categoricals."],["df[\"ward\"]","Values become the rows of the table."],["df[\"gender\"]","Values become the columns of the table."]],"requires":["two aligned categorical Series"],"important":["the joint distribution of two categories is the question","row or column proportions are wanted (use <code>normalize=</code>)"],"avoid":["high-cardinality inputs (the table becomes unreadable)","using it to aggregate a measure without <code>values</code> and <code>aggfunc</code>"],"reach":"When the relationship between two categorical variables must be shown as a contingency table.","example":"<pre>tab = pd.crosstab(df[<span class=\"s\">\"ward\"</span>], df[<span class=\"s\">\"gender\"</span>], normalize=<span class=\"s\">\"index\"</span>)</pre>","tags":["reshape","crosstab","contingency","frequency table","kreuztabelle"]},{"id":"explode","code":"df.explode(\"tags\")","stage":"reshape","short":"Expand list-valued cells into one row each.","what":"Takes a column whose cells are lists and produces one row per list element, repeating the other columns. The natural follow-up to <code>str.split</code> or <code>str.findall</code> when each original row should become several.","tokens":[["df.explode(","Expands list-like cells into separate rows."],["\"tags\"","The column whose lists are unpacked, one element per new row."]],"requires":["a column whose values are lists (or list-like)"],"important":["a multi-valued field must become one row per value for grouping or counting"],"avoid":["the column is not list-like (no expansion occurs)","forgetting that empty lists yield a single NaN row"],"reach":"When a column holds multiple values per row that must each become their own row.","example":"<pre>per_tag = df.assign(tags=df[<span class=\"s\">\"tags\"</span>].str.split(<span class=\"s\">\",\"</span>)).explode(<span class=\"s\">\"tags\"</span>)</pre>","tags":["reshape","explode","lists","one row per","split","aufteilen"]},{"id":"melt","code":"df.melt(id_vars=\"patient_id\", var_name=\"metric\", value_name=\"value\")","stage":"reshape","short":"Wide → long: collapse many columns into key/value rows.","what":"Unpivots a wide table: the columns named in <code>id_vars</code> are kept as identifiers, every other column becomes two columns — one holding the former column name, one holding its value. The inverse of <code>pivot</code>, and the step that turns a spreadsheet-shaped table into tidy long form.","tokens":[["df.melt(","Unpivots wide columns into long key/value rows."],["id_vars=\"patient_id\"","Column(s) that identify a row and are kept as-is."],["var_name=\"metric\"","Name for the new column holding the former column names."],["value_name=\"value\"","Name for the new column holding the values."]],"requires":["a wide DataFrame with several measure columns","the identifier column(s) to preserve"],"important":["a spreadsheet-shaped table must be made tidy (one observation per row) for plotting or grouping"],"avoid":["melting columns of incompatible meaning into one value column"],"reach":"When a wide, spreadsheet-style table must be converted to tidy long form before grouping or plotting.","example":"<pre>long = df.melt(id_vars=<span class=\"s\">\"patient_id\"</span>,\n               var_name=<span class=\"s\">\"metric\"</span>, value_name=<span class=\"s\">\"value\"</span>)</pre>","tags":["reshape","melt","wide to long","unpivot","tidy","umformen"]},{"id":"pivot","code":"df.pivot(index=\"date\", columns=\"ward\", values=\"los_days\")","stage":"reshape","short":"Long → wide: spread one column into many.","what":"Reshapes long data to wide: distinct values of <code>columns</code> become new column headers, indexed by <code>index</code>, filled from <code>values</code>. Unlike <code>pivot_table</code> it does not aggregate — it raises if an (index, column) pair is duplicated, which makes it a strict reshape.","tokens":[["df.pivot(","Spreads a long table into wide form (no aggregation)."],["index=\"date\"","Column whose values become the row index."],["columns=\"ward\"","Column whose distinct values become new columns."],["values=\"los_days\"","Column supplying the cell values."]],"requires":["long-format data","at most one value per (index, column) pair"],"important":["a tidy table must be turned into a wide display or matrix and no aggregation is wanted"],"avoid":["duplicate (index, column) pairs exist — <code>pivot</code> raises; use <code>pivot_table</code> with an <code>aggfunc</code> instead"],"reach":"When tidy long data must be spread into a wide matrix and each cell is uniquely determined.","example":"<pre>wide = df.pivot(index=<span class=\"s\">\"date\"</span>, columns=<span class=\"s\">\"ward\"</span>, values=<span class=\"s\">\"los_days\"</span>)</pre>","tags":["reshape","pivot","long to wide","spread","umformen"]},{"id":"stack","code":"wide.stack()","stage":"reshape","short":"Move column labels down into the row index.","what":"Compresses the columns into the index: the result is a Series (or DataFrame) with an additional innermost index level taken from the former column labels. The usual way to turn a wide matrix back into a long, indexed series. NaN cells are dropped by default.","tokens":[["wide","A wide DataFrame, often the result of a pivot."],[".stack()","Pushes the column labels into a new innermost index level."]],"requires":["a wide DataFrame (typically with a meaningful column index)"],"important":["a wide matrix must be returned to a long, multi-indexed Series for further computation"],"avoid":["expecting NaN cells to be retained (pass <code>dropna=False</code>)"],"reach":"When a wide table must be folded back into a long, indexed form — the inverse of unstack.","example":"<pre>long = wide.stack().rename(<span class=\"s\">\"value\"</span>).reset_index()</pre>","tags":["reshape","stack","wide to long","multiindex","umformen"]},{"id":"unstack","code":"grouped.unstack(\"ward\")","stage":"reshape","short":"Move an index level up into the columns.","what":"Pivots an index level into the columns — the inverse of <code>stack</code>. Most often applied to the MultiIndex Series produced by grouping on two keys, to obtain a readable wide table (one key down the rows, the other across the columns).","tokens":[["grouped","A Series/DataFrame with a MultiIndex, e.g. from a two-key groupby."],[".unstack(\"ward\")","Lifts the 'ward' index level up to become columns."]],"requires":["a MultiIndex with at least two levels"],"important":["a two-key grouped result must be presented as a wide cross-tabulation"],"avoid":["unstacking a high-cardinality level (produces an unmanageably wide table)"],"reach":"When a multi-key grouped Series must be reshaped into a readable wide table for display.","example":"<pre>table = (df.groupby([<span class=\"s\">\"month\"</span>, <span class=\"s\">\"ward\"</span>])[<span class=\"s\">\"los_days\"</span>]\n           .mean().unstack(<span class=\"s\">\"ward\"</span>))</pre>","tags":["reshape","unstack","long to wide","multiindex","pivot","umformen"]},{"id":"np_arange","code":"np.arange(0, 10, 2)","stage":"numpy","short":"Evenly spaced values by step.","what":"Returns an array from start (inclusive) to stop (exclusive) in increments of step — the array analogue of Python's <code>range</code>, but it accepts floats. For a fixed count of points use <code>linspace</code> instead.","tokens":[["np.arange(","Generates a range as an array."],["0, 10, 2","start (inclusive), stop (exclusive), step."]],"requires":["numpy imported"],"important":["a sequence defined by a step size is needed"],"avoid":["a fixed number of points spanning a range (use <code>linspace</code>)","relying on the exact endpoint with a float step (rounding)"],"reach":"When a regularly stepped numeric sequence is required.","example":"<pre>x = np.arange(<span class=\"s\">0</span>, <span class=\"s\">1</span>, <span class=\"s\">0.1</span>)</pre>","tags":["numpy","arange","range","sequence","step"]},{"id":"np_argsort","code":"np.argsort(a)","stage":"numpy","short":"Indices that would sort an array.","what":"Returns the array of indices that, used to index the array, yield it in sorted order. Use it to reorder one array by another, or to find the positions of the smallest/largest values without sorting the data itself.","tokens":[["np.argsort(","Returns sorting indices, not sorted values."],["a","The array to be ordered."]],"requires":["numpy imported","a 1-D array or Series"],"important":["another array must be reordered to match this one's sort order","the positions of the n smallest/largest are needed"],"avoid":["the sorted values themselves are wanted (use <code>np.sort</code> or <code>Series.sort_values</code>)"],"reach":"When ordering positions — rather than ordered values — are required, e.g. to align a second array.","example":"<pre>order = np.argsort(scores)[::<span class=\"s\">-1</span>]   <span class=\"c\"># indices, best first</span></pre>","tags":["numpy","argsort","indices","sort order","ranking"]},{"id":"np_array","code":"np.array([1, 2, 3])","stage":"numpy","short":"Create an ndarray from a sequence.","what":"Builds an n-dimensional array from a list (or list of lists). All elements share one dtype, which is what makes array operations vectorised and fast. A pandas column's underlying values are such an array (<code>df['c'].to_numpy()</code>).","tokens":[["np.array(","Constructs an ndarray."],["[1, 2, 3]","The source sequence; nesting lists creates further dimensions."]],"requires":["numpy imported (<code>import numpy as np</code>)","a sequence of values of compatible type"],"important":["explicit numeric array operations are needed outside a DataFrame"],"avoid":["mixing incompatible types (everything is coerced to one dtype, often object)"],"reach":"When numeric data must be held and operated on as a homogeneous array rather than a Python list.","example":"<pre>a = np.array([<span class=\"s\">1.0</span>, <span class=\"s\">2.0</span>, <span class=\"s\">3.0</span>])</pre>","tags":["numpy","array","ndarray","vector","create"]},{"id":"np_clip","code":"np.clip(a, lo, hi)","stage":"numpy","short":"Bound values to a range.","what":"Returns the array with every value below <code>lo</code> raised to <code>lo</code> and every value above <code>hi</code> lowered to <code>hi</code>. Used to cap outliers or enforce a valid domain (e.g. non-negative quantities).","tokens":[["np.clip(","Limits values to an interval."],["a","The array (or Series) to bound."],["lo, hi","The lower and upper limits."]],"requires":["numpy imported","a defensible lower and/or upper bound"],"important":["outliers must be capped (winsorising) or values forced into a valid range"],"avoid":["clipping away genuine signal without recording the decision"],"reach":"When values must be constrained to a plausible range, for instance to limit the influence of outliers.","example":"<pre>df[<span class=\"s\">\"cost_eur\"</span>] = np.clip(df[<span class=\"s\">\"cost_eur\"</span>], <span class=\"s\">0</span>, <span class=\"k\">None</span>)</pre>","tags":["numpy","clip","cap","winsorise","bound","begrenzen"]},{"id":"np_linspace","code":"np.linspace(0, 1, 50)","stage":"numpy","short":"A fixed number of evenly spaced values.","what":"Returns exactly <code>num</code> values evenly spaced from start to stop, both endpoints included by default. Preferred over <code>arange</code> when the count matters — for instance to build a smooth x-axis for a curve.","tokens":[["np.linspace(","Generates evenly spaced samples."],["0, 1, 50","start, stop (inclusive), and how many points."]],"requires":["numpy imported"],"important":["a precise number of points across an interval is needed (plot grid, parameter sweep)"],"avoid":["a step-defined sequence is wanted (use <code>arange</code>)"],"reach":"When a set number of evenly spaced points across a range is required.","example":"<pre>xs = np.linspace(<span class=\"s\">0</span>, <span class=\"s\">2*np.pi</span>, <span class=\"s\">200</span>)</pre>","tags":["numpy","linspace","evenly spaced","grid","points"]},{"id":"np_log_exp","code":"np.log(a)  /  np.exp(a)","stage":"numpy","short":"Natural logarithm and its inverse, element-wise.","what":"<code>np.log</code> applies the natural logarithm to every element; <code>np.exp</code> the inverse. A log transform compresses a right-skewed positive variable toward symmetry; <code>np.log1p</code> / <code>np.expm1</code> handle zeros safely.","tokens":[["np.log(a)","Natural log of each element (undefined for ≤ 0)."],["np.exp(a)","e raised to each element — the inverse of log."]],"requires":["numpy imported","for <code>log</code>, strictly positive values"],"important":["a positively skewed variable must be transformed toward symmetry before modelling or plotting"],"avoid":["<code>np.log</code> on zero or negative values (yields -inf or NaN — use <code>np.log1p</code>)"],"reach":"When a skewed positive variable should be transformed, or a log-scaled quantity returned to its original scale.","example":"<pre>df[<span class=\"s\">\"log_cost\"</span>] = np.log1p(df[<span class=\"s\">\"cost_eur\"</span>])</pre>","tags":["numpy","log","exp","transform","skew","logarithmus"]},{"id":"np_percentile","code":"np.percentile(a, [25, 50, 75])","stage":"numpy","short":"Percentile values of an array.","what":"Returns the value(s) below which the given percentage of the data falls — the array-level counterpart of <code>Series.quantile</code> (note percentile takes 0–100, quantile 0–1). Used for robust spread and threshold computation on plain arrays.","tokens":[["np.percentile(","Computes percentile values."],["a","The numeric array."],["[25, 50, 75]","The percentiles, on a 0–100 scale."]],"requires":["numpy imported","a numeric array"],"important":["robust spread (IQR) or thresholds are needed on a NumPy array rather than a Series"],"avoid":["passing fractions 0–1 (that is <code>quantile</code>, not <code>percentile</code>)"],"reach":"When percentile thresholds or a robust spread are needed for a plain array.","example":"<pre>p10, p90 = np.percentile(arr, [<span class=\"s\">10</span>, <span class=\"s\">90</span>])</pre>","tags":["numpy","percentile","quantile","iqr","threshold"]},{"id":"np_random_choice","code":"rng.choice(options, size=10, replace=False)","stage":"numpy","short":"Draw a random sample from a set of options.","what":"Selects <code>size</code> elements from the supplied array. <code>replace=False</code> samples without replacement (no element twice); <code>p=</code> supplies non-uniform selection probabilities. Called on a seeded generator for reproducibility.","tokens":[["rng.choice(","Draws random elements from an array."],["options","The pool to sample from."],["size=10","How many to draw."],["replace=False","Without replacement — each element at most once."]],"requires":["a seeded generator (<code>np.random.default_rng</code>)","a pool to sample from"],"important":["a random subsample or a randomised assignment is needed reproducibly"],"avoid":["<code>replace=False</code> with <code>size</code> larger than the pool (raises)"],"reach":"When a reproducible random sample or assignment must be drawn from a set of values.","example":"<pre>picks = rng.choice(df[<span class=\"s\">\"patient_id\"</span>], size=<span class=\"s\">20</span>, replace=<span class=\"k\">False</span>)</pre>","tags":["numpy","random","choice","sample","stichprobe"]},{"id":"np_random_rng","code":"rng = np.random.default_rng(seed=42)","stage":"numpy","short":"Create a seeded random number generator.","what":"Returns a Generator whose seed fixes its starting state, so every run produces the same sequence — essential for reproducible examples and simulations. Draw from it with <code>rng.integers</code>, <code>rng.normal</code>, <code>rng.choice</code>. The modern replacement for the legacy <code>np.random.seed</code> API.","tokens":[["np.random.default_rng(","Constructs the recommended modern generator."],["seed=42","Fixes the starting state for reproducibility."]],"requires":["numpy imported"],"important":["any analysis whose random results must be reproducible by a colleague","generating synthetic example data"],"avoid":["the legacy global <code>np.random.seed</code> / <code>np.random.rand</code> API in new code"],"reach":"Whenever randomness is used and the results must be reproducible.","example":"<pre>rng = np.random.default_rng(seed=<span class=\"s\">42</span>)\nsample = rng.normal(<span class=\"s\">0</span>, <span class=\"s\">1</span>, size=<span class=\"s\">100</span>)</pre>","tags":["numpy","random","default_rng","seed","reproducible","zufall"]},{"id":"np_reshape","code":"a.reshape(3, 4)  /  a.T","stage":"numpy","short":"Change an array's shape, or transpose it.","what":"<code>reshape</code> reinterprets the same data under a new shape whose element count must match (one dimension may be <code>-1</code> to be inferred). <code>.T</code> transposes — rows become columns. Both return views where possible, so changes can affect the original.","tokens":[["a.reshape(3, 4)","Reinterprets the data as a 3×4 array (total size must match)."],["a.T","Transposes the array (swaps axes)."]],"requires":["numpy imported","for reshape, a new shape with the same number of elements"],"important":["data must be arranged into a matrix for a linear-algebra or model API","rows and columns must be swapped"],"avoid":["a new shape inconsistent with the element count (raises)","assuming a copy — reshape often returns a view"],"reach":"When an array must be re-laid-out into a different shape or transposed for a downstream API.","example":"<pre>matrix = np.arange(<span class=\"s\">12</span>).reshape(<span class=\"s\">3</span>, <span class=\"s\">4</span>)</pre>","tags":["numpy","reshape","transpose","shape","matrix","umformen"]},{"id":"np_zeros_ones","code":"np.zeros((3, 4))  /  np.ones(5)","stage":"numpy","short":"Pre-allocate an array of zeros or ones.","what":"Creates an array of the given shape filled with 0.0 (<code>zeros</code>) or 1.0 (<code>ones</code>). Used to pre-allocate a result that a computation will fill, or to build a constant array. The shape is a tuple for more than one dimension.","tokens":[["np.zeros((3, 4))","A 3×4 array of zeros (shape given as a tuple)."],["np.ones(5)","A length-5 array of ones."]],"requires":["numpy imported","the desired shape"],"important":["a result array must be pre-allocated before a loop fills it","a constant array is needed as a starting point"],"avoid":["growing an array element by element instead of pre-allocating (slow)"],"reach":"When an array of known shape must be created before being filled or used as a constant.","example":"<pre>acc = np.zeros(<span class=\"s\">len</span>(groups))</pre>","tags":["numpy","zeros","ones","allocate","constant"]}],"glossary":[{"term":"accessor","syn":[".str",".dt",".cat","str accessor"],"def":"A namespace attached to a Series that exposes operations specific to its content type. <code>.str</code> exposes string methods (<code>strip</code>, <code>lower</code>); <code>.dt</code> exposes datetime methods (<code>year</code>, <code>month</code>); <code>.cat</code> exposes categorical operations. Methods called through an accessor are applied element-wise across the Series."},{"term":"alias","syn":["import as","abbreviation","abkuerzung"],"def":"A short name bound to a module by the <code>as</code> clause of an import. The conventions <code>import pandas as pd</code>, <code>import numpy as np</code>, <code>import matplotlib.pyplot as plt</code> are followed almost universally and should be retained."},{"term":"API","syn":["application programming interface","rest api"],"def":"An application programming interface — a defined contract by which one piece of software requests data or actions from another. In data work, frequently used to describe an HTTP-based service that returns JSON in response to a query."},{"term":"argument","syn":["argumente"],"def":"A value passed into a function call. <em>Positional</em> arguments are matched by position (the file path in <code>pd.read_csv(\"file.csv\")</code>); <em>keyword</em> arguments are matched by name (<code>sep=','</code>). The function's signature determines which positional arguments are required and which keyword arguments accept defaults."},{"term":"assertion","syn":["assert","check","pruefung","überprüfung"],"def":"A statement that raises an exception if a stated condition is false. Used as a self-check at the boundary between steps — for instance <code>assert df.shape[0] == 320</code> both documents and enforces the expected row count."},{"term":"attribute","syn":["attribut","property"],"def":"A value attached to an object, accessed without parentheses: <code>df.shape</code>. Attributes describe state; methods perform actions."},{"term":"axes","syn":["matplotlib axes","plot axes"],"def":"In Matplotlib, the coordinate system on which a single plot is drawn. A figure may contain one axes (single plot) or several (subplots). Plotting calls (<code>ax.bar</code>, <code>ax.plot</code>, …) are issued on an axes object rather than on the figure itself."},{"term":"axis","syn":["achse","dimension"],"def":"The dimension along which an operation is performed. <code>axis=0</code> moves down rows (column-wise reduction); <code>axis=1</code> moves across columns (row-wise reduction). For concatenation, the axis is the dimension that grows."},{"term":"bbox_inches","syn":["bounding box","savefig crop"],"def":"An argument to <code>plt.savefig</code> controlling the bounding box of the saved figure. <code>bbox_inches='tight'</code> crops surrounding whitespace so that axis labels, titles, and legends are not clipped at the image edge."},{"term":"boolean","syn":["bool","wahrheitswert","true false"],"def":"A value that is either <code>True</code> or <code>False</code>. Comparison operators (<code>==</code>, <code>&lt;</code>, <code>&gt;=</code>) produce booleans; pandas operations on whole columns produce boolean Series, which can in turn be used to filter rows."},{"term":"broadcasting","syn":["alignment"],"def":"The rule by which NumPy and pandas perform arithmetic between arrays of different but compatible shapes. A scalar combines with every element; a shorter array is virtually repeated to match a longer one."},{"term":"cardinality","syn":["m:1","1:m","1:1","kardinalitaet","kardinalität"],"def":"In a join, the number of rows on one side that correspond to a row on the other. Common cardinalities: <em>one-to-one</em> (each key appears at most once on either side), <em>one-to-many</em> (one row on the left corresponds to several on the right), <em>many-to-one</em> (several rows on the left share one row on the right). <code>validate=\"m:1\"</code> asserts the expected cardinality."},{"term":"chaining","syn":["method chaining","verkettung"],"def":"A sequence of method calls applied to successive results: <code>df.dropna().sort_values('x').head(10)</code>. Chaining keeps an intermediate-free style; each pandas method returns a new DataFrame, which is the next method's input."},{"term":"column","syn":["col","spalte","feature","variable"],"def":"A vertical slice of a DataFrame — one variable, one type. Accessed as <code>df['col']</code>."},{"term":"comprehension","syn":["list comprehension","listenkomprehension"],"def":"A concise expression that builds a list (or other collection) by iterating over an iterable: <code>[x**2 for x in range(10)]</code>. May include a condition: <code>[x for x in xs if x &gt; 0]</code>. Equivalent to a for-loop that appends, but more readable and slightly faster."},{"term":"copy vs view","syn":["copy","view","SettingWithCopyWarning"],"def":"Indexing operations sometimes return a view (a window into the original) and sometimes a copy. Assigning to a view modifies the original; assigning to a copy does not. The safe pattern for writing back is <code>df.loc[rows, col] = value</code>."},{"term":"crosstab","syn":["pd.crosstab","kreuztabelle","frequency table"],"def":"A frequency table of two categorical variables. <code>pd.crosstab(df['ward'], df['gender'])</code> returns a table whose rows are wards, columns are genders, and cells are counts."},{"term":"DataFrame","syn":["df","tabelle","table"],"def":"A two-dimensional, labelled table — the central pandas object. Conceptually a dictionary of columns, each column a Series sharing a common index."},{"term":"delimiter","syn":["separator","sep","trennzeichen"],"def":"The character that separates fields in a delimited text file. Comma in classical CSV, semicolon in many European exports, tab in TSV. Set explicitly via <code>sep=</code> when not the default comma."},{"term":"dictionary","syn":["dict","mapping","key-value"],"def":"A Python collection storing values under named keys: <code>{\"M\": \"male\", \"F\": \"female\"}</code>. Used as the mapping argument to <code>replace</code>, <code>rename</code>, <code>agg</code>, and many other pandas methods."},{"term":"DPI","syn":["dots per inch","resolution","aufloesung","auflösung"],"def":"Dots per inch — the resolution of a raster image. <code>dpi=200</code> in <code>plt.savefig</code> produces a sufficiently sharp image for slides; <code>dpi=300</code> is the typical print standard."},{"term":"dtype","syn":["data type","datentyp","type"],"def":"The data type of a column or Series — for example <code>int64</code>, <code>float64</code>, <code>object</code> (typically strings), <code>category</code>, <code>datetime64</code>. The dtype determines which operations are permitted."},{"term":"encoding","syn":["zeichensatz","kodierung","utf-8","latin-1"],"def":"The mapping between bytes on disk and characters in memory. UTF-8 covers most modern text; older Windows exports often use <code>cp1252</code> or <code>latin-1</code>."},{"term":"exception","syn":["error","fehler","raise"],"def":"A signal that something went wrong during execution. Pandas and NumPy raise specific exceptions (<code>KeyError</code>, <code>ValueError</code>, <code>MergeError</code>) that name the cause. Handled with <code>try / except</code> blocks where appropriate; otherwise read the message and the traceback for the source line."},{"term":"figsize","syn":["plot size","figure size","abbildungsgroesse"],"def":"The width and height of a Matplotlib figure in inches, passed as a tuple to <code>plt.subplots(figsize=(10, 6))</code>. Determines aspect ratio and reading comfort; <code>(10, 6)</code> is a sensible default for slide-ready figures."},{"term":"figure","syn":["fig","matplotlib figure","abbildung","canvas"],"def":"In Matplotlib, the outermost container — the canvas on which one or more axes are drawn. The figure is the object that <code>plt.savefig</code> writes; the axes are what individual plot calls draw on."},{"term":"function","syn":["funktion"],"def":"A named, reusable block of code. Functions take inputs (arguments), perform a computation, and may return an output. Defined with <code>def name(args): ...</code>."},{"term":"groupby","syn":["aggregation","gruppieren"],"def":"The operation of partitioning a DataFrame by the values of one or more key columns and applying a function within each partition. The split–apply–combine model."},{"term":"header","syn":["column names","kopfzeile"],"def":"The first row of a CSV file when it contains the column names. <code>pd.read_csv</code> treats row 0 as the header by default; <code>header=None</code> indicates that no header row is present, in which case columns may be named via <code>names=[...]</code>."},{"term":"import","syn":["importieren"],"def":"The statement that loads a module into the current namespace. <code>import pandas as pd</code> binds the pandas library to the alias <code>pd</code>."},{"term":"imputation","syn":["impute","imputed","fillna value","auffuellen","auffüllen"],"def":"The replacement of a missing value (<code>NaN</code>) by an estimated substitute — most often a central value of the column (median, mean) or a domain default. Imputation makes the decision visible; allowing <code>NaN</code> to propagate hides it inside downstream calculations."},{"term":"in-place","syn":["inplace","modify in place"],"def":"An operation that modifies an object directly rather than returning a new one. Many pandas methods accept an <code>inplace=True</code> argument, though the idiomatic style is to reassign the return value: <code>df = df.dropna()</code>."},{"term":"Index","syn":["row labels","zeilenindex"],"def":"The labels assigned to the rows (or, for columns, to the columns) of a DataFrame. By default a <code>RangeIndex</code> 0…n−1, but may be set to any meaningful key (date, identifier)."},{"term":"inner join","syn":["inner","schnittmenge"],"def":"A join that retains only rows whose key is present in both tables."},{"term":"JSON","syn":["javascript object notation"],"def":"A widely used text format for structured data: nested combinations of objects (key-value pairs), arrays, strings, numbers, booleans, and <code>null</code>. The default response format of most HTTP APIs. Read with <code>pd.read_json</code> or parsed with Python's built-in <code>json</code> module."},{"term":"key","syn":["join key","schluessel"],"def":"The column whose values determine which row of one table corresponds to which row of another in a merge."},{"term":"keyword argument","syn":["kwarg","named argument","schluesselwortargument"],"def":"An argument passed to a function by name: <code>encoding='utf-8'</code>. Keyword arguments may appear in any order after the positional arguments and typically have defaults that may be omitted."},{"term":"lambda","syn":["anonymous function","anonyme funktion","inline function"],"def":"A small, unnamed function defined inline: <code>lambda x: x.upper()</code>. Used where a one-line function is needed for a single call, as in <code>df['x'].apply(lambda v: v.strip())</code>."},{"term":"left join","syn":["left","links"],"def":"A join that retains every row of the left (calling) table, filling missing matches from the right table with <code>NaN</code>."},{"term":"mask","syn":["boolean mask","filter"],"def":"A boolean array (or Series) used to select rows. <code>df[mask]</code> returns the rows where <code>mask</code> is <code>True</code>."},{"term":"mean","syn":["average","durchschnitt","mittelwert","arithmetic mean"],"def":"The arithmetic average — the sum of values divided by the count. Sensitive to outliers: a single extreme value can shift the mean substantially. Compare with the median, which is robust."},{"term":"median","syn":["zentralwert","middle value"],"def":"The middle value when the column is sorted — half the values lie below it, half above. Robust to outliers: unlike the mean, it is not pulled by extreme values. Preferred over the mean for skewed distributions."},{"term":"method","syn":["methode"],"def":"A function attached to an object, called with dot notation: <code>df.head()</code>. The object on which it is called provides part of the input."},{"term":"mutable","syn":["mutable","immutable","mutability"],"def":"A mutable object can be changed in place (lists, dictionaries, DataFrames). An immutable object cannot (tuples, strings, integers). Mutability matters because mutable defaults in function definitions are shared across calls."},{"term":"NaN","syn":["null","na","missing","fehlend","lücke","leer"],"def":"«Not a Number» — the marker pandas uses for missing values. NaN is never equal to anything, including itself; missingness is tested with <code>isna()</code>, not <code>== NaN</code>."},{"term":"NaT","syn":["not a time"],"def":"The missing-value marker for datetime columns — the temporal analogue of <code>NaN</code>."},{"term":"normalise","syn":["normalize","standardise","normalisieren"],"def":"To bring values into a single, consistent form. In data cleaning: collapsing alternate spellings of a category to a canonical label. In statistics: rescaling values to a common range, such as 0–1 or zero-mean unit-variance."},{"term":"outer join","syn":["outer","union","vereinigung"],"def":"A join that retains every row from both tables, filling missing matches with <code>NaN</code>. Useful for auditing which keys are present in only one source."},{"term":"outlier","syn":["ausreisser","ausreißer","extreme value","extremwert"],"def":"A value that lies far from the bulk of the data. May indicate a measurement error, an input error, or a genuine rare event. Outliers disproportionately influence the mean, the range, and least-squares fits; the median and the quantiles are robust to them."},{"term":"parameter","syn":["argument","kwarg","keyword argument"],"def":"A named input to a function. <em>Positional</em> arguments are matched by position; <em>keyword</em> arguments by name (<code>sep=','</code>)."},{"term":"path","syn":["pfad","filepath"],"def":"The location of a file in the filesystem. Constructed with <code>pathlib.Path</code> for portability across operating systems."},{"term":"pip","syn":["package","paket"],"def":"Python's package installer. <code>pip install pandas</code> installs the pandas library into the active Python environment."},{"term":"pivot","syn":["wide","crosstab"],"def":"A reshaping that turns long-format data into wide format: distinct values of one variable become the column labels of the result."},{"term":"propagate","syn":["propagation","fortpflanzen","nan propagation"],"def":"To let a value travel through subsequent computations unchanged. With respect to <code>NaN</code>: a propagating operation returns <code>NaN</code> wherever an input was <code>NaN</code>. Pandas's <code>mean</code>, <code>sum</code>, and <code>groupby</code> do <em>not</em> propagate <code>NaN</code> by default — they skip it. The option <code>skipna=False</code> forces propagation, useful when the presence of any missingness should make the result itself missing."},{"term":"pseudo-random","syn":["random","seed","reproducibility","reproduzierbar","random seed"],"def":"Produced by a deterministic algorithm that, given a starting state (the <em>seed</em>), generates a sequence resembling randomness. Reproducible: the same seed yields the same sequence. Necessary for teaching examples, statistical simulations, and any analysis whose results must be verifiable."},{"term":"quartile","syn":["IQR","percentile","quantile","quantil","interquartile range"],"def":"Quartiles divide an ordered distribution into four equal parts: Q1 (25th percentile), Q2 (median), Q3 (75th percentile). The interquartile range (IQR) is Q3 − Q1, a robust measure of spread."},{"term":"raster","syn":["png","jpg","bitmap","pixel image"],"def":"An image stored as a grid of pixels; quality depends on resolution. PNG and JPG are raster formats. In contrast, vector formats (PDF, SVG) store geometric instructions and remain sharp at any size."},{"term":"recode","syn":["recoding","umkodieren","relabel"],"def":"To replace a value or category with another that better serves the analysis — for instance, mapping <code>NaN</code> to the explicit category <code>\"Unknown\"</code>, or collapsing several specific diagnoses into a broader class."},{"term":"reset_index","syn":["reindex","index zurücksetzen"],"def":"A pandas method that renumbers the rows to a clean <code>0…n-1</code> sequence. After filtering or dropping rows, the index has gaps; <code>reset_index(drop=True)</code> restores consecutive numbering and discards the old index instead of retaining it as an additional column."},{"term":"row","syn":["zeile","observation","record","case"],"def":"A horizontal slice of a DataFrame — one observation across all variables. Accessed by position with <code>df.iloc[i]</code> or by label with <code>df.loc[label]</code>."},{"term":"scalar","syn":["single value","skalar"],"def":"A single value, as opposed to an array or Series. A number, a string, a boolean — anything that is not a collection. Most NumPy and pandas operations broadcast scalars across the whole array or column."},{"term":"sentinel value","syn":["sentinel","platzhalter","-1","9999","-99","placeholder value"],"def":"A special, agreed-upon value used by a source system to signal \"no valid value here\" — typically because the export format cannot store <code>NaN</code> directly. Common conventions: <code>-1</code>, <code>-99</code>, <code>9999</code>, the string <code>\"N/A\"</code>. Sentinel values must be translated to <code>NaN</code> before analysis; otherwise they are treated as genuine measurements and distort every subsequent statistic."},{"term":"Series","syn":["spalte","vector","column"],"def":"A one-dimensional, labelled array — a single column of a DataFrame, or a standalone 1D structure. Carries both values and an index."},{"term":"shape","syn":["form","dimensions","groesse","größe"],"def":"The dimensions of an array or DataFrame as a tuple: <code>(rows, columns)</code> for a 2D structure, <code>(n,)</code> for a 1D Series or array. Accessed as the <code>.shape</code> attribute, without parentheses."},{"term":"skipna","syn":["skip na","skip nan"],"def":"An argument to many pandas aggregations controlling whether <code>NaN</code> values are excluded from the calculation. Default is <code>True</code> — <code>NaN</code> is ignored. <code>skipna=False</code> forces <code>NaN</code> to propagate, so the result is <code>NaN</code> whenever any input was <code>NaN</code>."},{"term":"slicing","syn":["slice","ausschnitt","subrange"],"def":"A selection of a subrange of a sequence or array using <code>start:stop:step</code> notation. <code>arr[2:7]</code> takes elements 2 through 6; <code>df.iloc[:, 1:4]</code> takes columns 1 through 3 of every row."},{"term":"split-apply-combine","syn":["groupby model"],"def":"The conceptual model of groupby: <em>split</em> the table by a key, <em>apply</em> a function to each group, <em>combine</em> the results into a single summary."},{"term":"subplot","syn":["axes grid","multiple plots","mehrere plots"],"def":"One of several axes arranged in a grid within a single figure. Created with <code>fig, axes = plt.subplots(2, 3)</code> for a 2×3 grid; each cell is then drawn on with <code>axes[i, j]</code>."},{"term":"subset","syn":["teilmenge","selection","portion"],"def":"A portion of a DataFrame — a selection of certain rows, columns, or both. Used as an argument name in pandas (e.g. <code>dropna(subset=['col'])</code> restricts the operation to specified columns)."},{"term":"tidy data","syn":["long format"],"def":"A convention in which each variable forms a column, each observation forms a row, and each type of observational unit forms a table. Pandas operations are most natural on tidy data."},{"term":"tight_layout","syn":["matplotlib spacing","auto layout"],"def":"A Matplotlib function that recomputes the spacing between axes elements so that titles, labels, and tick marks do not overlap. Called immediately before <code>savefig</code> or <code>show</code>."},{"term":"traceback","syn":["stack trace","fehlerstapel"],"def":"The chain of function calls that led to an exception, printed when an error occurs. Read from bottom upward: the last line names the exception type and its message; the lines above show the path through which the error was reached. The deepest entry inside the user's own code is usually where to start investigating."},{"term":"tuple","syn":["tupel","ordered pair"],"def":"An ordered, immutable Python sequence, written with parentheses: <code>(1, 2, 3)</code>. Used for fixed-length records where the position of each element is meaningful — for instance, the <code>.shape</code> of a DataFrame is a tuple."},{"term":"validate","syn":["merge validate","validate=","kardinalitätsprüfung"],"def":"A keyword argument to <code>pd.merge</code> that asserts the expected cardinality of the join (<code>'1:1'</code>, <code>'1:m'</code>, <code>'m:1'</code>, or <code>'m:m'</code>) and raises an exception if the assumption is violated. Recommended on any non-trivial merge as protection against silently inflated results."},{"term":"vectorisation","syn":["vectorization","vektorisierung"],"def":"The application of an operation to a whole array or column in a single step, dispatched to compiled code. Markedly faster than the equivalent Python loop."},{"term":"whitespace","syn":["leerzeichen","spaces","tabs","blank"],"def":"Characters that appear blank: ordinary spaces, tabs, and line breaks. Often present at the start or end of strings from manual data entry; removed by <code>.str.strip()</code> before grouping or comparison."}],"synonyms":{"remove":["drop","delete","kill","eliminate","loeschen","löschen","entfernen","wegwerfen"],"drop":["remove","delete","kill","loeschen","löschen","entfernen"],"missing":["nan","null","na","fehlend","lücke","luecke","leer","empty"],"row":["rows","zeile","zeilen","observation","record","case","fall"],"column":["columns","col","cols","spalte","spalten","feature","variable"],"average":["mean","durchschnitt","mittel","mittelwert"],"mean":["average","durchschnitt","mittelwert"],"group":["groupby","gruppe","gruppieren","per","by","aggregate","aggregation"],"aggregate":["groupby","aggregation","summary","summarise","zusammenfassen"],"merge":["join","verknuepfen","verknüpfen","verbinden","combine"],"combine":["merge","join","verbinden","kombinieren","complement","ergaenzen","ergänzen"],"concat":["concatenate","stack","stapeln","append","union"],"array":["arrays","ndarray","numpy"],"1d":["one-dimensional","one dimensional","eindimensional","vector"],"2d":["two-dimensional","two dimensional","zweidimensional","matrix","table"],"plot":["chart","graph","visualize","visualisieren","diagramm","abbildung"],"bar":["bar chart","balken","balkendiagramm"],"histogram":["hist","verteilung","distribution"],"line":["trend","linie","liniendiagramm"],"title":["titel","heading"],"label":["beschriftung","axis label","achsenbeschriftung"],"save":["export","write","schreiben","speichern"],"load":["read","import","einlesen","laden"],"filter":["subset","where","wo","auswaehlen","filtern"],"sort":["order","sortieren","ranking"],"rename":["umbenennen","relabel"],"type":["dtype","datatype","datentyp","cast"],"date":["datetime","datum","time"],"duplicate":["duplicates","duplikat","doppelt","duplicated"],"encoding":["kodierung","utf-8","latin-1","cp1252","zeichensatz"],"compare":["comparison","vergleich","vergleichen"],"count":["frequency","anzahl","haeufigkeit"],"outlier":["ausreisser","extreme"],"replace":["ersetzen","substitute","recode"],"fill":["impute","auffuellen","fillna"]},"tasks":{"datasets":{"encounters":{"label":"encounters — one row per hospital stay","setup":"import pandas as pd\n\ndf = pd.DataFrame({\n    \"ward\":      [\"ICU\", \"ICU\", \"Cardio\", \"Cardio\", \"Cardio\", \"Neuro\"],\n    \"age\":       [71, 64, None, 58, 81, 49],\n    \"los_days\":  [3, 9, 2, 5, 40, 4],\n    \"admit_date\": [\"2026-01-03\", \"2026-01-07\", \"2026-02-11\",\n                   \"2026-02-12\", \"2026-03-01\", \"2026-03-04\"],\n    \"diagnosis\": [\"sepsis\", \"Sepsis, severe\", \"angina\",\n                  \"MI\", \"sepsis\", \"stroke\"],\n})"},"revenue":{"label":"revenue — quarterly revenue by region","setup":"import pandas as pd\n\ndf = pd.DataFrame({\n    \"region\":  [\"North\", \"North\", \"South\", \"South\", \"East\", \"East\"],\n    \"quarter\": [\"Q1\", \"Q2\", \"Q1\", \"Q2\", \"Q1\", \"Q2\"],\n    \"revenue\": [120, 145, 98, 110, 210, 175],\n})"},"wide_measures":{"label":"wide_measures — one row per patient, measures in columns","setup":"import pandas as pd\n\ndf = pd.DataFrame({\n    \"patient\":   [\"p01\", \"p02\", \"p03\"],\n    \"systolic\":  [128, 141, 119],\n    \"diastolic\": [82, 90, 76],\n})"}},"tasks":[{"id":"q-idxmax-vs-max","type":"concept","stage":"stats","question":"Which method returns the <em>label</em> of the maximum value in a Series?","options":[".max()",".idxmax()",".argmax()",".loc[max]"],"correct":1,"why":"max returns the value itself; idxmax returns the index label of that value; argmax returns the integer position.","related":["max","idxmax","argmax"]},{"id":"q-median-vs-mean","type":"concept","stage":"stats","question":"A length-of-stay column has a few very long outlier stays. Which statistic best describes a <em>typical</em> stay?","options":["mean","median","sum","std"],"correct":1,"why":"The median is robust to outliers; the mean is pulled toward the long tail and overstates the typical stay.","related":["median","mean","quantile"]},{"id":"q-size-vs-count","type":"concept","stage":"aggregate","question":"Within a groupby, what is the difference between <code>size()</code> and <code>count()</code>?","options":["They are identical","size() includes NaN rows; count() excludes them","count() includes NaN rows; size() excludes them","size() works only on numeric columns"],"correct":1,"why":"size() counts all rows in each group including missing values; count() counts only non-null values per column.","related":["groupby_size","groupby_count"]},{"id":"q-loc-vs-iloc","type":"concept","stage":"select","question":"<code>df.iloc[0]</code> selects a row by what?","options":["Its index label","Its integer position","Its first column value","A boolean mask"],"correct":1,"why":"iloc is positional (0 = first row regardless of the index); loc is label-based.","related":["iloc","loc"]},{"id":"q-dropna-default","type":"concept","stage":"clean","question":"By default, <code>df.dropna()</code> removes a row if…","options":["all of its values are missing","any one of its values is missing","its index is missing","the first column is missing"],"correct":1,"why":"The default is how='any' — a single NaN anywhere in the row drops it. Use how='all' or subset= to narrow this.","related":["dropna","isna_sum"]},{"id":"q-merge-how","type":"concept","stage":"merge","question":"You want every row of the left table kept, with matching columns added from the right where a key exists. Which join?","options":["inner","left","right","outer"],"correct":1,"why":"A left join keeps all left rows; unmatched right columns become NaN. inner would drop unmatched left rows.","related":["merge","combine_first"]},{"id":"q-astype-str-num","type":"concept","stage":"clean","question":"A column of ages loaded as text contains the value \"unknown\". Which is the safest conversion?","options":["df['age'].astype(int)","pd.to_numeric(df['age'], errors='coerce')","df['age'].astype(float).astype(int)","int(df['age'])"],"correct":1,"why":"astype(int) raises on \"unknown\"; to_numeric with errors='coerce' turns unconvertible values into NaN so the rest survives.","related":["to_numeric","astype"]},{"id":"q-vectorise-where","type":"concept","stage":"transform","question":"To create a column that is \"high\" when value &gt; 100 else \"low\", the idiomatic tool is…","options":["a for-loop","np.where","df.apply with a lambda","df.iterrows()"],"correct":1,"why":"np.where is a vectorised if-else over the whole column — far faster and clearer than row-wise loops.","related":["np_where","np_select","new_col"]},{"id":"q-value-counts-normalize","type":"concept","stage":"stats","question":"Which gives the <em>proportion</em> of each category rather than the raw count?","options":["df['x'].value_counts()","df['x'].value_counts(normalize=True)","df['x'].nunique()","df['x'].count()"],"correct":1,"why":"normalize=True divides each count by the total, yielding proportions that sum to 1.","related":["value_counts_normalize","value_counts"]},{"id":"q-to-period","type":"concept","stage":"datetime","question":"For a monthly grouping key that sorts correctly across years, prefer…","options":["dt.month","dt.strftime('%m')","dt.to_period('M')","dt.year"],"correct":2,"why":"dt.month gives 1-12 with no year, so 2024-01 and 2025-01 collide; to_period('M') keeps year+month and sorts chronologically.","related":["dt_to_period","dt_month","resample"]},{"id":"c-median-per-group","type":"code","stage":"aggregate","dataset":"encounters","question":"Compute the median length of stay per ward as a Series.","hint":"Combine groupby with a column selection and median().","solution":"df.groupby(\"ward\")[\"los_days\"].median()","why":"Group-then-stat is the canonical split-apply-combine pattern; median is robust to outlier stays.","related":["groupby_mean","median"]},{"id":"c-drop-missing-age","type":"code","stage":"clean","dataset":"encounters","question":"Keep only rows where the <code>age</code> column is not missing.","hint":"dropna takes a subset= argument.","solution":"df.dropna(subset=[\"age\"])","why":"subset restricts the missing-value test to one column instead of dropping a row for any NaN anywhere.","related":["dropna","isna_sum"]},{"id":"c-filter-mask","type":"code","stage":"transform","dataset":"encounters","question":"Select all rows where <code>age</code> is at least 65.","hint":"Build a boolean mask and index with it.","solution":"df[df[\"age\"] >= 65]","why":"A boolean Series passed to df[...] keeps the rows where it is True — the standard filtering idiom.","related":["filter_mask","boolean_indexing","query"]},{"id":"c-top5-nlargest","type":"code","stage":"transform","dataset":"revenue","question":"Return the 3 rows with the highest <code>revenue</code>.","hint":"There is a dedicated method; no full sort needed.","solution":"df.nlargest(3, \"revenue\")","why":"nlargest is clearer and cheaper than sort_values(...).head(3) when you only need the top N.","related":["nlargest","sort_values"]},{"id":"c-parse-dates","type":"code","stage":"datetime","dataset":"encounters","question":"Convert the text column <code>admit_date</code> into real datetimes, in place.","hint":"pd.to_datetime, assigned back to the column.","solution":"df[\"admit_date\"] = pd.to_datetime(df[\"admit_date\"])","why":"Dates arrive as strings; parsing to datetime64 unlocks the .dt accessor, sorting, and time arithmetic.","related":["to_datetime","dt_year"]},{"id":"c-pivot-table","type":"code","stage":"aggregate","dataset":"revenue","question":"Build a table of mean <code>revenue</code> with <code>region</code> as rows and <code>quarter</code> as columns.","hint":"pivot_table with index, columns, values, aggfunc.","solution":"df.pivot_table(index=\"region\", columns=\"quarter\", values=\"revenue\", aggfunc=\"mean\")","why":"pivot_table produces a wide, display-ready summary and aggregates duplicates automatically.","related":["pivot_table","pivot"]},{"id":"c-str-contains","type":"code","stage":"strings","dataset":"encounters","question":"Select rows where <code>diagnosis</code> contains the word \"sepsis\" (case-insensitive).","hint":"Use the .str accessor; pass case and na arguments.","solution":"df[df[\"diagnosis\"].str.contains(\"sepsis\", case=False, na=False)]","why":"na=False prevents NaN cells from raising when the mask indexes rows; case=False makes the match case-insensitive.","related":["str_contains","boolean_indexing"]},{"id":"c-melt-long","type":"code","stage":"reshape","dataset":"wide_measures","question":"Reshape the wide table (id column <code>patient</code>) into long format.","hint":"melt with id_vars.","solution":"df.melt(id_vars=\"patient\")","why":"melt turns named measure columns into variable/value rows — the tidy layout grouping and plotting expect.","related":["melt","stack"]},{"id":"c-rng-seed","type":"code","stage":"numpy","question":"Create a reproducible random generator seeded with 42.","hint":"The modern NumPy API is np.random.default_rng.","solution":"rng = np.random.default_rng(42)","why":"A seeded default_rng makes random results reproducible; the legacy np.random.seed global state is discouraged.","related":["np_random_rng","np_random_choice"]},{"id":"r-transform-shape","type":"read","stage":"aggregate","dataset":"encounters","code":"df.groupby(\"ward\")[\"los_days\"].transform(\"mean\")","question":"What is the shape of the result?","options":["One row per ward (a small Series)","Same length as the original df (a Series of length n_rows)","A single scalar"],"correct":1,"why":"transform broadcasts the per-group statistic back to every original row, so the result aligns 1:1 with df.","related":["transform","groupby_mean"]},{"id":"r-chained-assign","type":"read","stage":"select","dataset":"encounters","code":"df[df[\"age\"] > 65][\"flag\"] = 1","question":"What happens?","options":["The flag column is set to 1 for those rows","A SettingWithCopyWarning; the assignment may not stick","A KeyError is raised","All rows get flag = 1"],"correct":1,"why":"Chained indexing assigns into a temporary copy. Use df.loc[df['age'] > 65, 'flag'] = 1 instead.","related":["copy_vs_view","loc","boolean_indexing"]},{"id":"r-nan-equality","type":"read","stage":"clean","code":"import numpy as np\nnp.nan == np.nan","question":"What does this evaluate to?","options":["True","False","NaN","It raises an error"],"correct":1,"why":"NaN is not equal to anything, including itself. Test for missingness with isna()/isnull(), never == np.nan.","related":["isna_sum","fillna"]},{"id":"r-merge-key-dtype","type":"read","stage":"merge","code":"left[\"id\"]  # dtype int64\nright[\"id\"] # dtype object ('001','002')\nleft.merge(right, on=\"id\")","question":"What is the likely outcome?","options":["A correct join on id","Zero matched rows — keys differ by dtype","A TypeError is raised immediately","Pandas auto-casts and it just works"],"correct":1,"why":"Integer 1 never equals string '001'. Align key dtypes before merging or the join silently produces no matches.","related":["merge","astype"]},{"id":"r-int-float-nan","type":"read","stage":"clean","code":"s = pd.Series([1, 2, None])\ns.dtype","question":"What is the dtype of s?","options":["int64","float64","object","Int64"],"correct":1,"why":"A classic int column with a missing value is upcast to float64, because NaN is a float. Use the nullable Int64 dtype to keep integers.","related":["astype","fillna","isna_sum"]}]},"decisions":{"lede":"Three decisions recur in any non-trivial analysis. Each admits several defensible answers; the correct answer depends on the question and on the structure of the missingness or the join.","boxes":[{"id":"missing-values","title":"Missing values — drop rows, drop columns, fill, or allow to propagate?","intro":"A column contains <code>NaN</code>. The decision is determined by two considerations: <em>why</em> the value is missing, and <em>how essential</em> the column is to the question.","options":[{"kind":"kill","label":"Drop the row — <code>dropna(subset=[col])</code>","body":"Indicated when the column is essential to the analysis (e.g. <code>patient_id</code>, <code>admission_date</code>) and no substitute is defensible. Acceptable if fewer than approximately five per cent of rows are lost."},{"kind":"kill","label":"Drop the column — <code>drop(columns=[col])</code>","body":"Indicated when the column is more than approximately half missing and not load-bearing for the question, or is redundant with another column. The decision should be recorded explicitly in the notebook."},{"kind":"fill","label":"Impute — <code>fillna(median)</code> / <code>ffill</code>","body":"Indicated when missingness is plausibly random, the column is numeric, and a substitute is defensible. The median is robust to skew; the mean only for symmetric distributions; forward-fill for time series whose state changes infrequently."},{"kind":"ignore","label":"Allow to propagate","body":"Indicated when the downstream aggregation is itself robust to <code>NaN</code> (pandas's <code>mean</code>, <code>sum</code>, and <code>groupby</code> skip <code>NaN</code> by default). Only when a <code>NaN</code>-propagating result is required does one specify <code>skipna=False</code>."}],"note":"The decision should be made <em>per column</em>, not per DataFrame. <code>dropna()</code> applied to the whole DataFrame, with default arguments, is almost always the wrong choice on real data."},{"id":"merging-dataframes","title":"Merging two DataFrames — which join, which method?","options":[{"kind":"fill","label":"Enrich table A with columns from B","code":"a.merge(b, on=key, how=\"left\")","body":". Retains every row of A; adds matching columns from B, with <code>NaN</code> where no match exists."},{"kind":"fill","label":"Restrict to rows present in both","code":"a.merge(b, on=key, how=\"inner\")","body":". Smaller result; no missing values introduced by the merge."},{"kind":"fill","label":"Union of all keys","code":"a.merge(b, on=key, how=\"outer\", indicator=True)","body":". Permits auditing of which keys are present in only one source."},{"kind":"fill","label":"A is the reference; B fills the gaps","code":"a.combine_first(b)","body":". Applied cell by cell: where A has a value, A is retained; where A is <code>NaN</code>, B is used."},{"kind":"fill","label":"Stack vertically (same columns)","code":"pd.concat([a, b], ignore_index=True)","body":"."},{"kind":"fill","label":"Glue side by side (same rows)","code":"pd.concat([a, b], axis=1)","body":"."}]},{"id":"numpy-combination","title":"NumPy — which array-combination function?","options":[{"kind":"fill","label":"1D + 1D, end to end","code":"np.concatenate([a,b])","body":" or <code>np.hstack([a,b])</code>. Produces <code>[a₀…aₙ, b₀…bₘ]</code>."},{"kind":"fill","label":"2D + 2D, stack as further rows","code":"np.concatenate([A,B], axis=0)","body":" or <code>np.vstack([A,B])</code>."},{"kind":"fill","label":"2D + 2D, glue as further columns","code":"np.concatenate([A,B], axis=1)","body":" or <code>np.hstack([A,B])</code>."},{"kind":"fill","label":"Two 1D arrays → 2D with each as a column","code":"np.column_stack([a,b])","body":". Shape <code>(n, 2)</code>."},{"kind":"fill","label":"Two 1D arrays → 2D with each as a row","code":"np.vstack([a,b])","body":". Shape <code>(2, n)</code>."},{"kind":"fill","label":"Introduce a new axis","code":"np.stack([a,b])","body":". Used for batching."}],"note":"<strong>Mnemonic:</strong> the argument <code>axis</code> denotes the dimension along which the result <em>grows</em>. <code>axis=0</code> produces additional rows; <code>axis=1</code> produces additional columns."}]},"example":{"context":"A short, self-contained analysis that traces all eight stages on synthetic data — generated inline so the script runs in any notebook without an external file. Two questions are addressed of the Saint Mary's data: <em>which wards have the longest length of stay</em>, and <em>which DRGs are loss-making</em>.","quickStages":"<strong>What the script does, by stage:</strong><br>\n(1) generates a small Saint Mary's dataset with deliberate irregularities;\n(2) inspects shape, types, and missingness;\n(3) cleans gender labels, sentinel values, and missing length of stay;\n(4) derives margin and a loss indicator;\n(5) aggregates length of stay by ward and margin by DRG;\n(6) merges encounters with the DRG catalogue and the patient register;\n(7) produces a two-panel figure;\n(8) writes the summaries and the enriched table to disk.","copyNote":"The script may be copied into a Jupyter notebook and executed top to bottom. It requires <code>pandas</code>, <code>numpy</code>, and <code>matplotlib</code> — already part of a standard scientific Python installation.","code":"<span class=\"c\"># ============================================================</span>\n<span class=\"c\"># Saint Mary's — a complete, runnable example</span>\n<span class=\"c\"># Question: Which wards have the longest length of stay,</span>\n<span class=\"c\">#           and which DRGs are loss-making?</span>\n<span class=\"c\"># ============================================================</span>\n\n<span class=\"k\">import</span> numpy <span class=\"k\">as</span> np\n<span class=\"k\">import</span> pandas <span class=\"k\">as</span> pd\n<span class=\"k\">import</span> matplotlib.pyplot <span class=\"k\">as</span> plt\n<span class=\"k\">from</span> pathlib <span class=\"k\">import</span> Path\n\n<span class=\"h\"># --- Step 1: data ----------------------------------------------</span>\n<span class=\"c\"># In production one would load files:</span>\n<span class=\"c\">#   patients   = pd.read_csv(\"data/patients.csv\")</span>\n<span class=\"c\">#   encounters = pd.read_csv(\"data/encounters.csv\")</span>\n<span class=\"c\"># Here the tables are generated so the script is self-contained.</span>\n\n<span class=\"c\"># np.random.default_rng creates a *pseudo-random number generator* — an</span>\n<span class=\"c\"># object from which subsequent calls draw random values. The 'seed'</span>\n<span class=\"c\"># argument fixes its starting state, so that every run produces the same</span>\n<span class=\"c\"># sequence of values. This is essential for reproducibility: a colleague</span>\n<span class=\"c\"># running the same script obtains the same example data.</span>\nrng = np.random.default_rng(seed=<span class=\"s\">42</span>)\n\npatients = pd.DataFrame({\n    <span class=\"s\">\"patient_id\"</span>: [<span class=\"s\">f\"P{i:03d}\"</span> <span class=\"k\">for</span> i <span class=\"k\">in</span> range(<span class=\"s\">60</span>)],\n    <span class=\"s\">\"age\"</span>: rng.integers(<span class=\"s\">18</span>, <span class=\"s\">90</span>, size=<span class=\"s\">60</span>),\n    <span class=\"s\">\"gender\"</span>: rng.choice(\n        np.array([<span class=\"s\">\"M\"</span>, <span class=\"s\">\"F\"</span>, <span class=\"s\">\"m\"</span>, <span class=\"s\">\"female\"</span>, <span class=\"s\">\"männlich\"</span>, <span class=\"s\">\" male  \"</span>, <span class=\"k\">None</span>], dtype=object),\n        size=<span class=\"s\">60</span>),\n})\n\nencounters = pd.DataFrame({\n    <span class=\"s\">\"encounter_id\"</span>: [<span class=\"s\">f\"E{i:04d}\"</span> <span class=\"k\">for</span> i <span class=\"k\">in</span> range(<span class=\"s\">100</span>)],\n    <span class=\"s\">\"patient_id\"</span>: rng.choice(patients[<span class=\"s\">\"patient_id\"</span>], size=<span class=\"s\">100</span>),\n    <span class=\"s\">\"ward\"</span>: rng.choice(\n        np.array([<span class=\"s\">\"ICU\"</span>, <span class=\"s\">\"Cardiology\"</span>, <span class=\"s\">\"Orthopaedics\"</span>, <span class=\"s\">\"General\"</span>, <span class=\"k\">None</span>], dtype=object),\n        size=<span class=\"s\">100</span>),\n    <span class=\"s\">\"drg_code\"</span>: rng.choice([<span class=\"s\">\"A01\"</span>, <span class=\"s\">\"A02\"</span>, <span class=\"s\">\"B05\"</span>, <span class=\"s\">\"C10\"</span>, <span class=\"s\">\"D12\"</span>], size=<span class=\"s\">100</span>),\n    <span class=\"s\">\"los_days\"</span>: rng.integers(<span class=\"s\">1</span>, <span class=\"s\">21</span>, size=<span class=\"s\">100</span>).astype(float),\n    <span class=\"s\">\"cost_eur\"</span>: rng.normal(<span class=\"s\">2500</span>, <span class=\"s\">800</span>, size=<span class=\"s\">100</span>).round(<span class=\"s\">2</span>),\n})\n<span class=\"c\"># Inject realistic irregularities</span>\nencounters.loc[rng.choice(<span class=\"s\">100</span>, <span class=\"s\">5</span>, replace=<span class=\"k\">False</span>), <span class=\"s\">\"los_days\"</span>] = np.nan\nencounters.loc[rng.choice(<span class=\"s\">100</span>, <span class=\"s\">3</span>, replace=<span class=\"k\">False</span>), <span class=\"s\">\"cost_eur\"</span>] = <span class=\"s\">-1</span>   <span class=\"c\"># sentinel for \"invalid\"</span>\n\ndrg_catalog = pd.DataFrame({\n    <span class=\"s\">\"drg_code\"</span>:          [<span class=\"s\">\"A01\"</span>, <span class=\"s\">\"A02\"</span>, <span class=\"s\">\"B05\"</span>, <span class=\"s\">\"C10\"</span>, <span class=\"s\">\"D12\"</span>],\n    <span class=\"s\">\"reimbursement_eur\"</span>: [<span class=\"s\">3200</span>, <span class=\"s\">2100</span>, <span class=\"s\">1800</span>, <span class=\"s\">4500</span>, <span class=\"s\">1500</span>],\n})\n\n<span class=\"h\"># --- Step 2: inspect -------------------------------------------</span>\nprint(<span class=\"s\">\"Shape:\"</span>, encounters.shape)\nprint(encounters.dtypes)\nprint(<span class=\"s\">\"Missing values per column:\"</span>)\nprint(encounters.isna().sum())\n\n<span class=\"h\"># --- Step 3: clean ---------------------------------------------</span>\n<span class=\"c\"># 3a — *normalise* the gender labels.</span>\n<span class=\"c\"># To normalise a column means to bring its values into a single, agreed</span>\n<span class=\"c\"># form. A single underlying category may appear in several spellings</span>\n<span class=\"c\"># (\"M\", \"m\", \"Male\", \"männlich\", with stray whitespace). Before any</span>\n<span class=\"c\"># grouping or comparison on 'gender' can be trusted, the strings must be</span>\n<span class=\"c\"># collapsed to a canonical form. The .str *accessor* applies string</span>\n<span class=\"c\"># methods element-wise to a Series:</span>\n<span class=\"c\">#   .strip()    removes leading and trailing whitespace;</span>\n<span class=\"c\">#   .lower()    converts every character to lowercase;</span>\n<span class=\"c\">#   .replace()  then maps the remaining variants onto two canonical labels.</span>\npatients[<span class=\"s\">\"gender\"</span>] = (patients[<span class=\"s\">\"gender\"</span>]\n                      .str.strip()\n                      .str.lower()\n                      .replace({<span class=\"s\">\"m\"</span>: <span class=\"s\">\"male\"</span>, <span class=\"s\">\"männlich\"</span>: <span class=\"s\">\"male\"</span>, <span class=\"s\">\"f\"</span>: <span class=\"s\">\"female\"</span>}))\n\n<span class=\"c\"># 3b — translate the *sentinel value* into NaN, then impute.</span>\n<span class=\"c\"># A sentinel value is a special, agreed-upon number that a source system</span>\n<span class=\"c\"># uses to signal \"no valid value here\" — most often because the export</span>\n<span class=\"c\"># file format cannot store NaN directly. Hospital information systems</span>\n<span class=\"c\"># frequently use -1, -99, or 9999 for this purpose. In the present data,</span>\n<span class=\"c\"># the cost export uses -1 for invalid entries.</span>\n<span class=\"c\">#</span>\n<span class=\"c\"># Until -1 is converted to NaN, the affected rows look like genuine costs</span>\n<span class=\"c\"># of minus one euro. Any subsequent statistic — the mean, the sum, the</span>\n<span class=\"c\"># distribution of margins — would be distorted by these pseudo-values.</span>\n<span class=\"c\">#</span>\n<span class=\"c\"># The two-step operation handles this:</span>\n<span class=\"c\">#   .replace({-1: np.nan}) substitutes the sentinel with np.nan, which is</span>\n<span class=\"c\">#                          pandas's proper marker for a missing value;</span>\n<span class=\"c\">#   .fillna(median)        then *imputes* (i.e. estimates and fills in)</span>\n<span class=\"c\">#                          those NaN cells using the column's median —</span>\n<span class=\"c\">#                          a robust central value that is unaffected by</span>\n<span class=\"c\">#                          outliers (the mean would be pulled by extremes).</span>\nencounters[<span class=\"s\">\"cost_eur\"</span>] = (encounters[<span class=\"s\">\"cost_eur\"</span>]\n                          .replace({<span class=\"s\">-1</span>: np.nan})\n                          .fillna(encounters[<span class=\"s\">\"cost_eur\"</span>].median()))\n\n<span class=\"c\"># 3c — length of stay is essential to the analysis, so rows that lack it</span>\n<span class=\"c\"># are dropped. After rows are dropped, the row labels (the *index*) have</span>\n<span class=\"c\"># gaps — 0, 1, 3, 4, 7, ... — because the dropped numbers are simply</span>\n<span class=\"c\"># absent. .reset_index(drop=True) renumbers the rows to a clean 0...n-1</span>\n<span class=\"c\"># sequence; 'drop=True' discards the old index instead of retaining it</span>\n<span class=\"c\"># as an additional column.</span>\nencounters = encounters.dropna(subset=[<span class=\"s\">\"los_days\"</span>]).reset_index(drop=<span class=\"k\">True</span>)\n\n<span class=\"c\"># 3d — a missing ward is informative in itself: it indicates that the</span>\n<span class=\"c\"># source system failed to record where the patient was treated. Rather</span>\n<span class=\"c\"># than discarding the row or imputing a fictitious ward, the absence is</span>\n<span class=\"c\"># *recoded* as the explicit category \"Unknown\". The downstream groupby</span>\n<span class=\"c\"># will then produce an \"Unknown\" group, making the volume of incomplete</span>\n<span class=\"c\"># records visible rather than hidden behind a NaN.</span>\nencounters[<span class=\"s\">\"ward\"</span>] = encounters[<span class=\"s\">\"ward\"</span>].fillna(<span class=\"s\">\"Unknown\"</span>)\n\n<span class=\"h\"># --- Step 4: transform -----------------------------------------</span>\n<span class=\"c\"># The margin requires the reimbursement, which lives in the DRG catalogue.</span>\n<span class=\"c\"># It is therefore computed after the merge in Step 6.</span>\n\n<span class=\"h\"># --- Step 5: aggregate (preview, before merge) -----------------</span>\n<span class=\"c\"># Average length of stay per ward — answers the first question.</span>\nlos_by_ward = (encounters\n               .groupby(<span class=\"s\">\"ward\"</span>)[<span class=\"s\">\"los_days\"</span>]\n               .agg([<span class=\"s\">\"mean\"</span>, <span class=\"s\">\"median\"</span>, <span class=\"s\">\"count\"</span>])\n               .round(<span class=\"s\">2</span>)\n               .sort_values(<span class=\"s\">\"mean\"</span>, ascending=<span class=\"k\">False</span>))\nprint(<span class=\"s\">\"\\nLength of stay by ward:\\n\"</span>, los_by_ward)\n\n<span class=\"h\"># --- Step 6: merge ---------------------------------------------</span>\n<span class=\"c\"># Two consecutive *left joins* enrich the encounter table with information</span>\n<span class=\"c\"># from the DRG catalogue and the patient register.</span>\n<span class=\"c\">#   how=\"left\"      retains every row of the left-hand table (the encounters)</span>\n<span class=\"c\">#                   and adds the matching columns from the right-hand table;</span>\n<span class=\"c\">#                   rows of the right table without a matching key are not</span>\n<span class=\"c\">#                   added (no row is duplicated, no row is lost on the left).</span>\n<span class=\"c\">#   validate=\"m:1\"  asserts the expected *cardinality* of the join — that</span>\n<span class=\"c\">#                   is, how many rows on one side correspond to how many on</span>\n<span class=\"c\">#                   the other. \"m:1\" reads as \"many-to-one\": many encounters</span>\n<span class=\"c\">#                   share one DRG catalogue entry; many encounters share one</span>\n<span class=\"c\">#                   patient record. If the assumption is violated (for</span>\n<span class=\"c\">#                   example a duplicate patient_id in 'patients'), pandas</span>\n<span class=\"c\">#                   raises an exception immediately, catching errors that</span>\n<span class=\"c\">#                   would otherwise silently inflate the result by joining</span>\n<span class=\"c\">#                   each encounter to multiple rows.</span>\nenriched = (encounters\n            .merge(drg_catalog, on=<span class=\"s\">\"drg_code\"</span>,   how=<span class=\"s\">\"left\"</span>, validate=<span class=\"s\">\"m:1\"</span>)\n            .merge(patients,    on=<span class=\"s\">\"patient_id\"</span>, how=<span class=\"s\">\"left\"</span>, validate=<span class=\"s\">\"m:1\"</span>))\n\n<span class=\"c\"># Column arithmetic in pandas is *vectorised*: the subtraction is applied</span>\n<span class=\"c\"># to every row in a single operation dispatched to compiled code, not by</span>\n<span class=\"c\"># a Python loop. This is markedly faster and more concise than iterating.</span>\n<span class=\"c\"># The comparison ' &lt; 0 ' similarly produces a boolean Series — True where</span>\n<span class=\"c\"># the margin is negative, False otherwise.</span>\nenriched[<span class=\"s\">\"margin_eur\"</span>] = enriched[<span class=\"s\">\"reimbursement_eur\"</span>] - enriched[<span class=\"s\">\"cost_eur\"</span>]\nenriched[<span class=\"s\">\"is_loss\"</span>]    = enriched[<span class=\"s\">\"margin_eur\"</span>] &lt; <span class=\"s\">0</span>\n\nmargin_by_drg = (enriched\n                 .groupby(<span class=\"s\">\"drg_code\"</span>)\n                 .agg(total_margin=(<span class=\"s\">\"margin_eur\"</span>, <span class=\"s\">\"sum\"</span>),\n                      n_cases    =(<span class=\"s\">\"encounter_id\"</span>, <span class=\"s\">\"count\"</span>))\n                 .round(<span class=\"s\">2</span>)\n                 .sort_values(<span class=\"s\">\"total_margin\"</span>))\nprint(<span class=\"s\">\"\\nMargin by DRG:\\n\"</span>, margin_by_drg)\n\n<span class=\"h\"># --- Step 7: visualise -----------------------------------------</span>\nfig, axes = plt.subplots(<span class=\"s\">1</span>, <span class=\"s\">2</span>, figsize=(<span class=\"s\">12</span>, <span class=\"s\">5</span>))\n\naxes[<span class=\"s\">0</span>].bar(los_by_ward.index, los_by_ward[<span class=\"s\">\"mean\"</span>], color=<span class=\"s\">\"#1565C0\"</span>)\naxes[<span class=\"s\">0</span>].set_title(<span class=\"s\">\"Average length of stay by ward\"</span>)\naxes[<span class=\"s\">0</span>].set_xlabel(<span class=\"s\">\"Ward\"</span>)\naxes[<span class=\"s\">0</span>].set_ylabel(<span class=\"s\">\"Mean length of stay (days)\"</span>)\naxes[<span class=\"s\">0</span>].tick_params(axis=<span class=\"s\">\"x\"</span>, rotation=<span class=\"s\">20</span>)\n\n<span class=\"c\"># A *list comprehension* produces one colour per DRG: red where the total</span>\n<span class=\"c\"># margin is negative, green where it is positive. Visual encoding of the</span>\n<span class=\"c\"># sign communicates the result more directly than the bar length alone.</span>\ncolors = [<span class=\"s\">\"#C62828\"</span> <span class=\"k\">if</span> v &lt; <span class=\"s\">0</span> <span class=\"k\">else</span> <span class=\"s\">\"#2E7D32\"</span>\n          <span class=\"k\">for</span> v <span class=\"k\">in</span> margin_by_drg[<span class=\"s\">\"total_margin\"</span>]]\naxes[<span class=\"s\">1</span>].barh(margin_by_drg.index, margin_by_drg[<span class=\"s\">\"total_margin\"</span>], color=colors)\n<span class=\"c\"># axvline draws a vertical reference line on the second axes; here at x=0,</span>\n<span class=\"c\"># marking the boundary between loss-making and profitable DRGs.</span>\naxes[<span class=\"s\">1</span>].axvline(<span class=\"s\">0</span>, color=<span class=\"s\">\"black\"</span>, linewidth=<span class=\"s\">0.8</span>)\naxes[<span class=\"s\">1</span>].set_title(<span class=\"s\">\"Total margin by DRG\"</span>)\naxes[<span class=\"s\">1</span>].set_xlabel(<span class=\"s\">\"Margin (EUR)\"</span>)\n\n<span class=\"c\"># tight_layout reconciles overlapping labels by recomputing the spacing</span>\n<span class=\"c\"># between axes. savefig then writes the figure to disk:</span>\n<span class=\"c\">#   dpi=200             sets a high raster resolution (dots per inch);</span>\n<span class=\"c\">#   bbox_inches=\"tight\" crops away surrounding whitespace, ensuring that</span>\n<span class=\"c\">#                       rotated tick labels and titles are not cut off.</span>\n<span class=\"c\"># savefig must be called *before* plt.show(), since plt.show may clear</span>\n<span class=\"c\"># the figure on some backends, leaving savefig to write an empty canvas.</span>\nplt.tight_layout()\nplt.savefig(<span class=\"s\">\"saint_marys_summary.png\"</span>, dpi=<span class=\"s\">200</span>, bbox_inches=<span class=\"s\">\"tight\"</span>)\nplt.show()\n\n<span class=\"h\"># --- Step 8: export --------------------------------------------</span>\nPath(<span class=\"s\">\"output\"</span>).mkdir(exist_ok=<span class=\"k\">True</span>)\nlos_by_ward.to_csv(<span class=\"s\">\"output/los_by_ward.csv\"</span>)\nmargin_by_drg.to_csv(<span class=\"s\">\"output/margin_by_drg.csv\"</span>)\nenriched.to_csv(<span class=\"s\">\"output/encounters_enriched.csv\"</span>, index=<span class=\"k\">False</span>)\n\nprint(<span class=\"s\">\"\\nDone. Summaries and the enriched table are in ./output/\"</span>)","footerNote":"The synthetic data is regenerated with a fixed random seed, so the numerical results are reproducible. To work with the actual Saint Mary's files, replace Step 1 with <code>pd.read_csv(...)</code> calls against <code>data/patients.csv</code>, <code>data/encounters.csv</code>, and <code>data/drg_catalog.csv</code>."}};
